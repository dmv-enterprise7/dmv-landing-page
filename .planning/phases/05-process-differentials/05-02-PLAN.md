---
phase: 05-process-differentials
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - index.html
  - styles.css
  - script.js
autonomous: true
requirements:
  - DIFF-01
  - DIFF-02
  - DIFF-03

must_haves:
  truths:
    - "4 differentials display in a 2x2 grid on desktop with a section title and subtitle"
    - "Each differential has a 48px SVG icon on the left, a bold title, and a 2-line description"
    - "Differentials use glassmorphism card styling consistent with services and pain points sections"
    - "All text toggles correctly between PT and EN via the language toggle"
    - "On mobile (768px and below), the 2x2 grid stacks to a single column"
  artifacts:
    - path: "index.html"
      provides: "Differentials section HTML: 4 diff-card elements in diff-grid, inline SVG icons, data-i18n attributes"
      contains: "diff-grid"
    - path: "styles.css"
      provides: "Differentials CSS: 2x2 grid layout, glassmorphism cards, icon styling, hover states, responsive"
      contains: "section-differentials"
    - path: "script.js"
      provides: "i18n keys for subtitle, 4 differential titles and descriptions in both PT and EN"
      contains: "diff.item1.title"
  key_links:
    - from: "index.html"
      to: "styles.css"
      via: "class names: diff-grid, diff-card, diff-icon, diff-title, diff-desc"
      pattern: "diff-grid"
    - from: "index.html"
      to: "script.js"
      via: "data-i18n attributes on subtitle and all 4 differential text elements"
      pattern: "data-i18n=\"diff\\."
---

<objective>
Build the "Por que a DMV?" differentials section with 4 real differentials in a 2x2 glassmorphism grid — each with an inline SVG icon, bold title, and 2-line description.

Purpose: This section kills the "why not a competitor" objection. Four concrete, honest differentials communicate what sets DMV apart without fabricated testimonials or inflated metrics.
Output: Fully styled, responsive, bilingual differentials section inside the existing #differentials element.
</objective>

<execution_context>
@C:/Users/00amiri/Documents/DMV Ent/Projetos DMV/dmv-landing-page/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/00amiri/Documents/DMV Ent/Projetos DMV/dmv-landing-page/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-process-differentials/05-CONTEXT.md
@index.html
@styles.css
@script.js

<interfaces>
<!-- CSS custom properties — same design tokens used throughout -->
```css
:root {
  --color-primary: #040810;
  --color-cyan: #00c8ff;
  --color-text: #e8ecf4;
  --color-text-secondary: #8a95a8;
  --glass-bg: rgba(255, 255, 255, 0.04);
  --glass-border: rgba(255, 255, 255, 0.08);
  --glass-blur: 16px;
  --font-headline: 'Sora', sans-serif;
  --font-body: 'DM Sans', sans-serif;
  --section-padding: clamp(60px, 10vw, 140px);
  --container-max: 1200px;
  --container-padding: clamp(16px, 4vw, 40px);
  --transition-default: 0.3s ease;
}
```

<!-- Glassmorphism card pattern from service-card (Phase 4, 04-02-SUMMARY.md) -->
```css
/* Established pattern — diff-cards should match this visual style -->
.service-card {
  background: var(--glass-bg);
  border: 1px solid var(--glass-border);
  backdrop-filter: blur(var(--glass-blur));
  -webkit-backdrop-filter: blur(var(--glass-blur));
  border-radius: 20px;
  padding: 32px 28px;
}
.service-card:hover {
  border-color: rgba(0, 200, 255, 0.25);
  box-shadow: 0 0 30px rgba(0, 200, 255, 0.1), 0 4px 20px rgba(0, 0, 0, 0.2);
  transform: translateY(-4px);
}
/* Service icon container: 48px, border-radius 14px, rgba(0,200,255,0.08) bg */
.service-icon {
  width: 48px;
  height: 48px;
  background: rgba(0, 200, 255, 0.08);
  border-radius: 14px;
  color: var(--color-cyan);
}
```

<!-- Pain points 2x2 grid pattern (Phase 4, 04-01) — use same for differentials -->
```css
.pain-points-grid {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 20px;
  width: 100%;
  max-width: 800px;
  margin-bottom: 48px;
}
```

<!-- Existing #differentials placeholder in index.html (lines 237-241) -->
```html
<section id="differentials" class="section section-differentials">
  <div class="container">
    <h2 data-i18n="diff.title">Por que a DMV?</h2>
  </div>
</section>
```

<!-- Existing i18n stub keys already present in script.js -->
PT: 'diff.title': 'Por que a DMV?'
EN: 'diff.title': 'Why DMV?'
<!-- These stubs already exist — do NOT add them again. Only add new keys after them. -->

<!-- 05-01 output available after plan 05-01 executes -->
<!-- how-it-works-steps, step-card CSS classes now in styles.css -->
<!-- how.* i18n keys now in script.js -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Differentials HTML + i18n keys</name>
  <files>index.html, script.js</files>
  <action>
**read_first:** index.html (lines 237-241 for #differentials placeholder), script.js (translations object — find `diff.title` in both pt and en blocks for insertion points)

**HTML (index.html):** Replace the content INSIDE `<section id="differentials" class="section section-differentials">` (keep the section tag and class intact, replace what's inside the container div). Full replacement:

```html
<section id="differentials" class="section section-differentials">
  <div class="container">
    <div class="diff-header">
      <h2 data-i18n="diff.title">Por que a DMV?</h2>
      <p class="diff-subtitle" data-i18n="diff.subtitle">Nao somos mais uma agencia. Somos parceiros de resultado.</p>
    </div>

    <div class="diff-grid">

      <div class="diff-card">
        <div class="diff-icon">
          <svg width="28" height="28" viewBox="0 0 28 28" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
            <path d="M14 4L4 9v10l10 5 10-5V9L14 4z"/>
            <path d="M14 4v20"/>
            <path d="M4 9l10 5 10-5"/>
          </svg>
        </div>
        <div class="diff-body">
          <h3 class="diff-title" data-i18n="diff.item1.title">100% Personalizado</h3>
          <p class="diff-desc" data-i18n="diff.item1.desc">Nenhum sistema identico ao outro. Cada solucao e criada do zero para o seu negocio.</p>
        </div>
      </div>

      <div class="diff-card">
        <div class="diff-icon">
          <svg width="28" height="28" viewBox="0 0 28 28" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
            <circle cx="14" cy="10" r="4"/>
            <path d="M6 24c0-4.418 3.582-8 8-8s8 3.582 8 8"/>
          </svg>
        </div>
        <div class="diff-body">
          <h3 class="diff-title" data-i18n="diff.item2.title">Atendimento Humano e Proximo</h3>
          <p class="diff-desc" data-i18n="diff.item2.desc">Voce fala diretamente com quem cria. Sem intermediarios, sem rodeios.</p>
        </div>
      </div>

      <div class="diff-card">
        <div class="diff-icon">
          <svg width="28" height="28" viewBox="0 0 28 28" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
            <polyline points="4 18 10 12 14 16 20 8 24 12"/>
            <line x1="24" y1="20" x2="4" y2="20"/>
          </svg>
        </div>
        <div class="diff-body">
          <h3 class="diff-title" data-i18n="diff.item3.title">Foco em Resultado Pratico</h3>
          <p class="diff-desc" data-i18n="diff.item3.desc">Nao vendemos tecnologia por tecnologia. Cada sistema tem um ROI claro.</p>
        </div>
      </div>

      <div class="diff-card">
        <div class="diff-icon">
          <svg width="28" height="28" viewBox="0 0 28 28" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
            <circle cx="14" cy="14" r="10"/>
            <polyline points="14 8 14 14 18 16"/>
            <path d="M20 4l2 2-2 2"/>
            <path d="M8 4L6 6l2 2"/>
          </svg>
        </div>
        <div class="diff-body">
          <h3 class="diff-title" data-i18n="diff.item4.title">Funciona 24 horas, 7 dias</h3>
          <p class="diff-desc" data-i18n="diff.item4.desc">Seus assistentes nunca dormem. Atendimento e automacao rodando sempre.</p>
        </div>
      </div>

    </div>
  </div>
</section>
```

**JavaScript (script.js):** Add the following NEW i18n keys to BOTH language blocks. Insert after the existing `'diff.title'` key in each language block. The stub `'diff.title'` already exists — only insert new keys after it.

PT keys to add after `'diff.title': 'Por que a DMV?',`:
```javascript
'diff.subtitle': 'Nao somos mais uma agencia. Somos parceiros de resultado.',
'diff.item1.title': '100% Personalizado',
'diff.item1.desc': 'Nenhum sistema identico ao outro. Cada solucao e criada do zero para o seu negocio.',
'diff.item2.title': 'Atendimento Humano e Proximo',
'diff.item2.desc': 'Voce fala diretamente com quem cria. Sem intermediarios, sem rodeios.',
'diff.item3.title': 'Foco em Resultado Pratico',
'diff.item3.desc': 'Nao vendemos tecnologia por tecnologia. Cada sistema tem um ROI claro.',
'diff.item4.title': 'Funciona 24 horas, 7 dias',
'diff.item4.desc': 'Seus assistentes nunca dormem. Atendimento e automacao rodando sempre.',
```

EN keys to add after `'diff.title': 'Why DMV?',`:
```javascript
'diff.subtitle': 'We\'re not just another agency. We\'re your results partner.',
'diff.item1.title': '100% Custom-Built',
'diff.item1.desc': 'No two systems are alike. Every solution is built from scratch for your business.',
'diff.item2.title': 'Human and Personal Support',
'diff.item2.desc': 'You talk directly to the people who build it. No middlemen, no runaround.',
'diff.item3.title': 'Focused on Practical Results',
'diff.item3.desc': 'We don\'t sell technology for technology\'s sake. Every system has a clear ROI.',
'diff.item4.title': 'Works 24 Hours, 7 Days',
'diff.item4.desc': 'Your assistants never sleep. Service and automation running at all times.',
```

**IMPORTANT:** Keep all existing keys untouched. Do NOT add `diff.title` again (already exists in both blocks).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const h=fs.readFileSync('index.html','utf8'); const j=fs.readFileSync('script.js','utf8'); const hChecks=['diff-grid','diff-card','diff-icon','diff-body','diff-title','diff-desc','diff-header','diff.item1.title','diff.item2.title','diff.item3.title','diff.item4.title','diff.subtitle']; const jChecks=['diff.item1.title','diff.item1.desc','diff.item2.title','diff.item2.desc','diff.item3.title','diff.item3.desc','diff.item4.title','diff.item4.desc','diff.subtitle','100% Custom-Built','Human and Personal Support','Practical Results','24 Hours, 7 Days']; const hFails=hChecks.filter(c=>!h.includes(c)); const jFails=jChecks.filter(c=>!j.includes(c)); const fails=[...hFails,...jFails]; if(fails.length){console.error('MISSING:',fails);process.exit(1)}else{console.log('ALL CHECKS PASSED: HTML structure and i18n keys present')}"</automated>
  </verify>
  <done>
    - index.html #differentials has a diff-header with title (data-i18n="diff.title") and subtitle (data-i18n="diff.subtitle")
    - 4 diff-card elements inside diff-grid, each with diff-icon (inline SVG) + diff-body (diff-title + diff-desc) with data-i18n
    - Icons represent: 3D box/personalization, person/human, line chart/results, clock/always-on
    - script.js has 9 PT keys and 9 EN keys added: diff.subtitle + diff.item1-4.title + diff.item1-4.desc
    - EN translations are idiomatic and natural
  </done>
</task>

<task type="auto">
  <name>Task 2: Differentials CSS — 2x2 glassmorphism grid, icon layout, hover states, responsive</name>
  <files>styles.css</files>
  <action>
**read_first:** styles.css (last ~30 lines to find end of file for clean append; also check the how-it-works section CSS added by 05-01 is present to confirm correct append location)

**Append the following CSS block at the end of styles.css**, after the how-it-works section CSS added in plan 05-01. Use the clear comment header shown.

```css
/* ============================================
   DIFFERENTIALS SECTION — "Por que a DMV?" (DIFF-01 through DIFF-03)
   ============================================ */

/* --- Section wrapper --- */

.section-differentials {
  position: relative;
  text-align: center;
}

/* --- Section header --- */

.diff-header {
  margin-bottom: 56px;
}

.section-differentials h2 {
  font-size: clamp(1.8rem, 4vw, 3rem);
  font-weight: 800;
  letter-spacing: -0.02em;
  margin-bottom: 16px;
}

.diff-subtitle {
  font-family: var(--font-body);
  font-size: clamp(1rem, 1.5vw, 1.15rem);
  color: var(--color-text-secondary);
  max-width: 480px;
  margin: 0 auto;
  line-height: 1.6;
}

/* --- 2x2 differentials grid (DIFF-01) --- */

.diff-grid {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 20px;
  width: 100%;
  max-width: 900px;
  margin: 0 auto;
}

/* --- Individual differential card: glassmorphism (DIFF-03) --- */

.diff-card {
  background: var(--glass-bg);
  border: 1px solid var(--glass-border);
  -webkit-backdrop-filter: blur(var(--glass-blur));
  backdrop-filter: blur(var(--glass-blur));
  border-radius: 20px;
  padding: 32px 28px;
  display: flex;
  align-items: flex-start;
  gap: 20px;
  text-align: left;
  transition: border-color var(--transition-default),
              box-shadow var(--transition-default),
              transform var(--transition-default);
}

.diff-card:hover {
  border-color: rgba(0, 200, 255, 0.25);
  box-shadow: 0 0 30px rgba(0, 200, 255, 0.1), 0 4px 20px rgba(0, 0, 0, 0.2);
  transform: translateY(-4px);
}

/* --- Icon container: 48px left-aligned (DIFF-03) --- */

.diff-icon {
  flex-shrink: 0;
  display: flex;
  align-items: center;
  justify-content: center;
  width: 48px;
  height: 48px;
  background: rgba(0, 200, 255, 0.08);
  border: 1px solid rgba(0, 200, 255, 0.12);
  border-radius: 14px;
  color: var(--color-cyan);
}

/* --- Text body --- */

.diff-body {
  flex: 1;
  min-width: 0;
}

/* --- Differential title: bold (DIFF-03) --- */

.diff-title {
  font-family: var(--font-headline);
  font-size: clamp(1rem, 1.4vw, 1.1rem);
  font-weight: 700;
  color: var(--color-text);
  margin-bottom: 10px;
  letter-spacing: -0.01em;
  line-height: 1.3;
}

/* --- Differential description: ~2 lines --- */

.diff-desc {
  font-family: var(--font-body);
  font-size: clamp(0.88rem, 1.1vw, 0.93rem);
  color: var(--color-text-secondary);
  line-height: 1.6;
  margin: 0;
}

/* --- Responsive: tablet — 2x2 still works, tighter padding --- */

@media (max-width: 1024px) {
  .diff-grid {
    max-width: 720px;
  }

  .diff-card {
    padding: 24px 22px;
  }
}

/* --- Responsive: mobile — single column stack (DIFF-01) --- */

@media (max-width: 768px) {
  .diff-grid {
    grid-template-columns: 1fr;
    gap: 16px;
    max-width: 440px;
  }

  .diff-header {
    margin-bottom: 40px;
  }

  .diff-card {
    padding: 22px 18px;
    gap: 16px;
  }
}

@media (max-width: 375px) {
  .diff-icon {
    width: 42px;
    height: 42px;
    border-radius: 12px;
  }

  .diff-title {
    font-size: 0.95rem;
  }

  .diff-desc {
    font-size: 0.85rem;
  }
}
```

**Key design decisions:**
- 2x2 CSS Grid with `repeat(2, 1fr)` and `max-width: 900px` — satisfies DIFF-01. Wider than pain-points grid (800px) to accommodate the icon-left layout comfortably.
- Cards use identical glassmorphism tokens to service-card (`var(--glass-bg)`, `var(--glass-border)`, `backdrop-filter: blur(var(--glass-blur))`, `border-radius: 20px`) — visual consistency with Phase 4 (per CONTEXT.md "glassmorphism cards consistent with services and pain points")
- `.diff-card` uses `align-items: flex-start` + `gap: 20px` for icon-left layout — satisfies DIFF-03
- `.diff-icon` is 48px with `rgba(0,200,255,0.08)` background and `border-radius: 14px` — matches the 48px service icon established in Phase 4
- Hover: same triple-effect as service cards (cyan border, box-shadow glow, translateY -4px)
- Mobile stacks to `grid-template-columns: 1fr` at 768px — no horizontal overflow at 375px
- NO entry animations (deferred to Phase 7 — per CONTEXT.md deferred section)
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const css=fs.readFileSync('styles.css','utf8'); const checks=['section-differentials','diff-grid','diff-card','diff-icon','diff-body','diff-title','diff-desc','repeat(2, 1fr)','backdrop-filter: blur','border-radius: 20px','diff-card:hover','max-width: 768px','grid-template-columns: 1fr']; const fails=checks.filter(c=>!css.includes(c)); if(fails.length){console.error('MISSING CSS:',fails);process.exit(1)}else{console.log('ALL CSS CHECKS PASSED')}"</automated>
  </verify>
  <done>
    - .diff-grid uses CSS Grid repeat(2, 1fr) for 2x2 desktop layout
    - .diff-card uses full glassmorphism tokens: glass-bg, glass-border, backdrop-filter, border-radius 20px
    - .diff-icon is 48px square with cyan background tint, flex-shrink: 0 (left-aligned)
    - .diff-title is Sora bold, .diff-desc is DM Sans with color-text-secondary
    - Hover effect: cyan border + glow shadow + translateY(-4px)
    - At 768px, grid becomes single column (1fr) and stacks
    - No animation CSS added (deferred to Phase 7)
  </done>
</task>

</tasks>

<verification>
1. Open index.html in browser — #differentials section shows 4 cards in a 2x2 grid below the how-it-works section
2. Each card has a cyan icon on the left, a bold title, and a 2-line description
3. Cards have visible glassmorphism (dark translucent background with subtle border)
4. Hover over a card — border becomes cyan, card lifts slightly (translateY), glow shadow appears
5. Toggle language to EN — subtitle and all 4 differential titles and descriptions switch to English
6. Resize to 768px — 2x2 grid collapses to single-column vertical stack
</verification>

<success_criteria>
- 4 differential cards render in 2x2 grid on desktop
- Each card has 48px SVG icon (left), bold title, 2-line description in the glassmorphism card style
- Visual style is consistent with services cards from Phase 4 (same tokens, same hover behavior)
- Language toggle switches all 9 text keys (subtitle + 4 titles + 4 descs)
- Single-column stack on mobile (768px and below)
- No entry animations present (those belong to Phase 7)
</success_criteria>

<output>
After completion, create `.planning/phases/05-process-differentials/05-02-SUMMARY.md`
</output>
