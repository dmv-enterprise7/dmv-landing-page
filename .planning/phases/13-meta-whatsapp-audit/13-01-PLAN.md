---
phase: 13-meta-whatsapp-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
autonomous: true
requirements:
  - META-01
  - META-02
  - META-03
  - META-04

must_haves:
  truths:
    - "Page <title> reflects consultative positioning (Consultoria e Automação com IA sob Medida)"
    - "Meta description reads the diagnosis-first copy locked in CONTEXT"
    - "OG and Twitter meta tags exist and match the new title + description"
    - "Every wa.me link in index.html and script.js resolves to 5521973495805 (zero stale numbers)"
    - "Footer navigation mirrors navbar (includes Setores) and every anchor scrolls to an existing section ID"
  artifacts:
    - path: "index.html"
      provides: "Updated <head> meta tags (title, description, og:*, twitter:*) and audited wa.me links + footer nav"
      contains: "Consultoria e Automação com IA sob Medida"
  key_links:
    - from: "index.html <head>"
      to: "search engines and social previews"
      via: "meta tags (title, description, og:title, og:description, twitter:card, twitter:title, twitter:description)"
      pattern: "property=\"og:title\"|name=\"twitter:title\""
    - from: "index.html footer nav + navbar + all CTAs"
      to: "WhatsApp (https://wa.me/5521973495805)"
      via: "href attribute"
      pattern: "wa\\.me/5521973495805"
    - from: "index.html footer <nav class=\"footer-nav\">"
      to: "page section IDs (#why-ai, #services, #sectors, #how-it-works, #about, #faq)"
      via: "anchor href"
      pattern: "href=\"#(why-ai|services|sectors|how-it-works|about|faq)\""
---

<objective>
Refresh SEO/social meta tags to reflect v2.0 consultative positioning, and audit every WhatsApp link + footer anchor across index.html and script.js to confirm zero stale content remains.

Purpose: Search engines, social previews, and every CTA must speak the new language and hit the correct number. This is the final quality gate for v2.0.
Output: Updated `<head>` in `index.html` with consultative meta tags + OG/Twitter variants, plus a clean audit confirming all wa.me links and footer anchors are correct.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-meta-whatsapp-audit/13-CONTEXT.md
@index.html
@script.js

<audit_snapshot>
<!-- Pre-planning audit (2026-04-15) — confirms scope of work -->

wa.me links in index.html (7 total, ALL already correct = 5521973495805):
- Line 48  : navbar CTA
- Line 69  : hero primary CTA
- Line 133 : market section CTA
- Line 380 : process section CTA
- Line 562 : final CTA
- Line 600 : footer WhatsApp icon
- Line 615 : floating WhatsApp button

wa.me references in script.js: 0 (no templated links)
Legacy number patterns (5521 variations other than 5521973495805): 0 found

Current meta tags (index.html lines 6-7 — NEED REPLACEMENT):
  <title>DMV Enterprise</title>
  <meta name="description" content="Agência de Automação com IA para pequenos e médios negócios. Assistentes virtuais, automação de processos e web apps customizadas.">

OG / Twitter meta tags: ABSENT — must be added.

Footer nav (index.html lines 591-598) — already includes Setores; anchors match section IDs in the DOM (#why-ai, #services, #sectors, #how-it-works, #about, #faq). Navbar (lines 36-41) mirrors it. Verification only.
</audit_snapshot>

<locked_copy>
<!-- From CONTEXT.md - D-01 meta tags (NON-NEGOTIABLE) -->

Title (PT canonical):
  DMV Enterprise — Consultoria e Automação com IA sob Medida

Meta description (PT canonical):
  Consultoria e automação com IA sob medida. Diagnosticamos seus processos, identificamos gargalos e implementamos soluções inteligentes para o seu negócio.

OG title = same as <title>
OG description = same as meta description
Twitter card = summary_large_image; same title + description

Canonical WhatsApp: https://wa.me/5521973495805
</locked_copy>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite head meta tags + add OG/Twitter variants</name>
  <files>index.html</files>
  <action>
Replace lines 6-7 of `index.html` with the consultative meta tag block below, placed inside `<head>` before the favicon link. Use exact copy locked in CONTEXT (D-01) — do NOT paraphrase.

Replacement block:

```html
  <title>DMV Enterprise — Consultoria e Automação com IA sob Medida</title>
  <meta name="description" content="Consultoria e automação com IA sob medida. Diagnosticamos seus processos, identificamos gargalos e implementamos soluções inteligentes para o seu negócio.">

  <!-- Open Graph / Facebook -->
  <meta property="og:type" content="website">
  <meta property="og:title" content="DMV Enterprise — Consultoria e Automação com IA sob Medida">
  <meta property="og:description" content="Consultoria e automação com IA sob medida. Diagnosticamos seus processos, identificamos gargalos e implementamos soluções inteligentes para o seu negócio.">
  <meta property="og:locale" content="pt_BR">

  <!-- Twitter -->
  <meta name="twitter:card" content="summary_large_image">
  <meta name="twitter:title" content="DMV Enterprise — Consultoria e Automação com IA sob Medida">
  <meta name="twitter:description" content="Consultoria e automação com IA sob medida. Diagnosticamos seus processos, identificamos gargalos e implementamos soluções inteligentes para o seu negócio.">
```

Rationale: PT is the default language (`<html lang="pt-BR">`); a single canonical PT meta set is correct per D-01 (Claude's Discretion allowed single-lang; we choose single-lang PT for simplicity — EN variants would require JS-driven swap which is out of scope for an audit phase). Do NOT add og:image or twitter:image — no social preview asset exists in the repo and referencing a missing image would hurt, not help, previews.
  </action>
  <verify>
    <automated>grep -c "Consultoria e Automação com IA sob Medida" index.html | grep -q "^3$" &amp;&amp; grep -c "property=\"og:title\"" index.html | grep -q "^1$" &amp;&amp; grep -c "name=\"twitter:card\"" index.html | grep -q "^1$" &amp;&amp; echo META_OK</automated>
  </verify>
  <done>
`<title>` and `<meta name="description">` match the CONTEXT D-01 locked copy exactly. OG tags (og:type, og:title, og:description, og:locale) and Twitter tags (twitter:card, twitter:title, twitter:description) exist in `<head>`. No og:image or twitter:image referenced (since no asset). Old "Agência de Automação" description is gone.
  </done>
</task>

<task type="auto">
  <name>Task 2: Audit + verify wa.me links and footer navigation sync</name>
  <files>index.html</files>
  <action>
This is an audit task. No substantive edits are expected — the pre-planning audit (see audit_snapshot) confirmed all wa.me links are already `5521973495805` and the footer nav already mirrors the navbar (including Setores). This task RE-VERIFIES the state at execution time and remediates any drift.

Step 1 — WhatsApp number audit:
  - grep -n "wa\.me" index.html script.js
  - Every match MUST be `https://wa.me/5521973495805`. If any stale number (e.g., `5522981113794`, other variants) appears, replace with the canonical number.
  - grep for legacy patterns in script.js: `grep -nE "55[0-9]{10,11}" script.js` — must return no matches (zero templated numbers in JS).

Step 2 — Footer nav sync audit:
  - Open `index.html` footer block (around line 591-598). Confirm entries: Por que IA? (#why-ai), Serviços (#services), Setores (#sectors), Como Funciona (#how-it-works), Sobre Nós (#about), FAQ (#faq).
  - For each anchor, confirm the target section ID exists in the DOM:
    `for id in why-ai services sectors how-it-works about faq; do grep -q "id=\"$id\"" index.html &amp;&amp; echo "$id OK" || echo "$id MISSING"; done`
  - If any ID is missing, either (a) fix the footer href to the correct ID, or (b) flag for user — but per audit_snapshot all IDs already exist.
  - Confirm each footer-nav `<a>` has a `data-i18n` attribute matching a key present in both PT and EN dictionaries in `script.js` (the existing set: nav.why, nav.services, nav.sectors, nav.how, nav.about, nav.faq — already verified present in both languages in script.js lines 11-16 and 141-147).

Step 3 — Navbar ↔ Footer mirror check:
  - Extract navbar links from `index.html` lines ~36-41 and footer-nav links from ~591-598. Confirm the set of `data-i18n` keys is identical (order may vary). Any mismatch → align footer to navbar.

Remediation rule: make the minimum edit necessary. If the audit passes with zero findings (expected based on pre-planning snapshot), leave `index.html` unchanged for this task — Task 1 alone is the substantive change in this plan.
  </action>
  <verify>
    <automated>test $(grep -oE "wa\.me/[0-9]+" index.html script.js | sort -u | wc -l) -eq 1 &amp;&amp; grep -q "wa.me/5521973495805" index.html &amp;&amp; ! grep -qE "55[0-9]{10,11}" script.js &amp;&amp; for id in why-ai services sectors how-it-works about faq; do grep -q "id=\"$id\"" index.html || exit 1; done &amp;&amp; for key in nav.why nav.services nav.sectors nav.how nav.about nav.faq; do grep -q "data-i18n=\"$key\"" index.html || exit 1; done &amp;&amp; echo AUDIT_OK</automated>
  </verify>
  <done>
Exactly one unique wa.me number appears across index.html + script.js, and it is `5521973495805`. Zero legacy Brazilian phone patterns exist in script.js. All 6 footer anchor IDs (#why-ai, #services, #sectors, #how-it-works, #about, #faq) resolve to existing section IDs in the DOM. Footer nav data-i18n keys match navbar keys one-to-one. No stale content remains anywhere on the page.
  </done>
</task>

</tasks>

<verification>
Global phase checks (run after both tasks complete):

1. `grep -c "Consultoria e Automação com IA sob Medida" index.html` returns 3 (title + og:title + twitter:title).
2. `grep -oE "wa\.me/[0-9]+" index.html script.js | sort -u` returns exactly one line: `wa.me/5521973495805`.
3. Open `index.html` in a browser, DevTools → Elements → `<head>`: title and description visible and match CONTEXT D-01.
4. Click each footer nav link → page smooth-scrolls to the corresponding section (manual spot check, 30 seconds).
5. View-source and confirm no leftover `5522981113794` or other numbers from earlier phases.
</verification>

<success_criteria>
- `<title>` = "DMV Enterprise — Consultoria e Automação com IA sob Medida"
- `<meta name="description">` = "Consultoria e automação com IA sob medida. Diagnosticamos seus processos, identificamos gargalos e implementamos soluções inteligentes para o seu negócio."
- OG tags (og:type, og:title, og:description, og:locale) present and matching
- Twitter tags (twitter:card, twitter:title, twitter:description) present and matching
- Zero stale WhatsApp numbers anywhere in index.html or script.js
- Footer nav mirrors navbar with working Setores anchor
- Phase 13 closes v2.0 milestone with zero legacy copy or links remaining
</success_criteria>

<output>
After completion, create `.planning/phases/13-meta-whatsapp-audit/13-01-SUMMARY.md` using the summary template.
</output>
