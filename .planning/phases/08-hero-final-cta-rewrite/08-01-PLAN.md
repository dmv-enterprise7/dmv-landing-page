---
phase: 08-hero-final-cta-rewrite
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
  - script.js
autonomous: true
requirements:
  - HERO-V2-01
  - HERO-V2-02
  - HERO-V2-03
  - HERO-V2-04
  - HERO-V2-05

must_haves:
  truths:
    - "Visitor sees hero headline 'Antes de automatizar, a gente entende onde seu negocio trava' in PT"
    - "Visitor sees consultative sub-headline mentioning diagnostico/gargalos in PT"
    - "Visitor sees primary CTA button reading 'Agende seu Diagnostico Gratuito' in PT"
    - "Visitor sees secondary CTA 'Veja como funciona' anchored to #how-it-works"
    - "Visitor sees 3 hero badges: 'Diagnostico 100% gratuito', 'Sem compromisso', 'Resposta imediata'"
    - "Hero primary CTA href resolves to https://wa.me/5521973495805"
    - "Switching language to EN updates every hero element to the EN consultative copy"
  artifacts:
    - path: "index.html"
      provides: "Hero section markup with updated default text (fallback) and preserved data-i18n keys"
      contains: "id=\"hero\""
    - path: "script.js"
      provides: "translations.pt and translations.en hero.* keys updated to consultative copy"
      contains: "hero.headline"
  key_links:
    - from: "index.html hero section data-i18n attributes"
      to: "script.js translations object"
      via: "setLanguage() lookup by key"
      pattern: "data-i18n=\"hero\\.(headline|subheadline|cta-primary|cta-secondary|seal-[1-3])\""
    - from: "index.html hero primary CTA"
      to: "WhatsApp diagnostic URL"
      via: "href attribute"
      pattern: "href=\"https://wa\\.me/5521973495805\""
    - from: "index.html hero secondary CTA"
      to: "process section anchor"
      via: "href anchor"
      pattern: "href=\"#how-it-works\""
---

<objective>
Rewrite the hero section (index.html markup default text + script.js PT/EN translations) to communicate the consultative positioning: "diagnosis first, then technology". No CSS changes. Preserve all existing classes, structure, and animations.

Purpose: The hero is the first conversion moment. Today's copy sells "AI on autopilot"; the new copy sells "we diagnose your business before proposing technology". This bridges the v1 execution-agency frame to the v2 consulting-first positioning.

Output: Updated index.html hero section (lines ~60-85) and updated hero.* keys in script.js translations (both pt and en blocks). HERO-V2-01 through HERO-V2-05 satisfied.
</objective>

<execution_context>
@C:/Users/00amiri/Documents/DMV Ent/Projetos DMV/dmv-landing-page/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/00amiri/Documents/DMV Ent/Projetos DMV/dmv-landing-page/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-hero-final-cta-rewrite/08-CONTEXT.md

<interfaces>
<!-- Current hero section structure in index.html (lines 60-85) -->

```html
<section id="hero" class="section section-hero">
  ...
  <span class="hero-badge" data-i18n="hero.badge">...</span>
  <h1 class="hero-headline" data-i18n="hero.headline">...</h1>
  <p class="hero-subheadline" data-i18n="hero.subheadline">...</p>
  <a href="https://wa.me/5521973495805" class="btn-cta-hero" data-i18n="hero.cta-primary" ...>...</a>
  <a href="#how-it-works" class="hero-cta-secondary" data-i18n="hero.cta-secondary">...</a>
  <span class="hero-seal-item">... <span data-i18n="hero.seal-1">...</span></span>
  <span class="hero-seal-item">... <span data-i18n="hero.seal-2">...</span></span>
  <span class="hero-seal-item">... <span data-i18n="hero.seal-3">...</span></span>
</section>
```

<!-- Current i18n keys in script.js (hero block only — both pt and en objects): -->
<!-- 'hero.badge', 'hero.headline', 'hero.subheadline', 'hero.cta-primary', 'hero.cta-secondary', 'hero.seal-1', 'hero.seal-2', 'hero.seal-3' -->

<!-- i18n mechanism: setLanguage(lang) replaces el.textContent for every [data-i18n] element using translations[lang][key]. So markup default text is just a fallback; the authoritative copy lives in script.js. BOTH must be updated to stay consistent. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update hero i18n keys in script.js (PT + EN)</name>
  <files>script.js</files>
  <read_first>
    - script.js (lines 1-237) — understand existing translations object structure and exact hero.* keys
  </read_first>
  <action>
    In script.js, update the hero section keys inside BOTH `translations.pt` and `translations.en` objects. Keep `hero.badge` exactly as-is (not in v2 scope — don't touch it). Replace the following keys only.

    PT block (satisfies HERO-V2-01 through HERO-V2-05):
    - `'hero.headline'`: `'Antes de automatizar, a gente entende onde seu negócio trava'`
    - `'hero.subheadline'`: `'Fazemos o diagnóstico dos seus processos, identificamos gargalos e só então desenhamos a IA sob medida que seu negócio realmente precisa.'`
    - `'hero.cta-primary'`: `'Agende seu Diagnóstico Gratuito'`
    - `'hero.cta-secondary'`: `'Veja como funciona \u2193'`  (keep the ↓ arrow via unicode escape to match existing style)
    - `'hero.seal-1'`: `'Diagnóstico 100% gratuito'`
    - `'hero.seal-2'`: `'Sem compromisso'`
    - `'hero.seal-3'`: `'Resposta imediata'`

    EN block (mirror consultative tone):
    - `'hero.headline'`: `'Before automating, we understand where your business gets stuck'`
    - `'hero.subheadline'`: `'We diagnose your processes, identify bottlenecks, and only then design the tailor-made AI your business really needs.'`
    - `'hero.cta-primary'`: `'Schedule Your Free Diagnosis'`
    - `'hero.cta-secondary'`: `'See how it works \u2193'`
    - `'hero.seal-1'`: `'100% free diagnosis'`
    - `'hero.seal-2'`: `'No commitment'`
    - `'hero.seal-3'`: `'Immediate response'`

    Preserve all surrounding keys, comments, object structure, and trailing commas exactly as-is. Do not touch `hero.badge`, market, pain, services, how, diff, about, faq, cta, footer, or nav keys in this task.
  </action>
  <verify>
    <automated>grep -c "Antes de automatizar, a gente entende onde seu negócio trava" script.js &amp;&amp; grep -c "Before automating, we understand where your business gets stuck" script.js &amp;&amp; grep -c "Agende seu Diagnóstico Gratuito" script.js &amp;&amp; grep -c "Schedule Your Free Diagnosis" script.js &amp;&amp; grep -c "Diagnóstico 100% gratuito" script.js &amp;&amp; node -e "require('./script.js')" 2>&amp;1 | head -5 || node --check script.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep "'hero.headline': 'Antes de automatizar, a gente entende onde seu negócio trava'" script.js` returns 1 match
    - `grep "'hero.headline': 'Before automating, we understand where your business gets stuck'" script.js` returns 1 match
    - `grep "'hero.subheadline': 'Fazemos o diagnóstico dos seus processos, identificamos gargalos e só então desenhamos a IA sob medida que seu negócio realmente precisa.'" script.js` returns 1 match
    - `grep "'hero.subheadline': 'We diagnose your processes, identify bottlenecks, and only then design the tailor-made AI your business really needs.'" script.js` returns 1 match
    - `grep "'hero.cta-primary': 'Agende seu Diagnóstico Gratuito'" script.js` returns 1 match
    - `grep "'hero.cta-primary': 'Schedule Your Free Diagnosis'" script.js` returns 1 match
    - `grep "'hero.cta-secondary': 'Veja como funciona \\\\u2193'" script.js` returns 1 match
    - `grep "'hero.cta-secondary': 'See how it works \\\\u2193'" script.js` returns 1 match
    - `grep "'hero.seal-1': 'Diagnóstico 100% gratuito'" script.js` returns 1 match
    - `grep "'hero.seal-1': '100% free diagnosis'" script.js` returns 1 match
    - `grep "'hero.seal-2': 'Sem compromisso'" script.js` returns 1 match
    - `grep "'hero.seal-2': 'No commitment'" script.js` returns 1 match
    - `grep "'hero.seal-3': 'Resposta imediata'" script.js` returns 1 match
    - `grep "'hero.seal-3': 'Immediate response'" script.js` returns 1 match
    - `node --check script.js` exits 0 (no syntax errors)
    - OLD copy REMOVED: `grep "Seu negócio no piloto automático com Inteligência Artificial" script.js` returns 0 matches
    - OLD copy REMOVED: `grep "Reunião 100% gratuita" script.js` returns 0 matches
  </acceptance_criteria>
  <done>
    Both `translations.pt.hero.*` and `translations.en.hero.*` keys (headline, subheadline, cta-primary, cta-secondary, seal-1/2/3) reflect the consultative v2 copy. `hero.badge` untouched. Script still parses.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update hero markup default text in index.html</name>
  <files>index.html</files>
  <read_first>
    - index.html (lines 60-85) — current hero section markup
    - script.js (lines 20-30, 132-141) — canonical PT/EN hero copy already updated in Task 1
  </read_first>
  <action>
    In index.html, update the default (fallback) text inside the hero section's data-i18n elements to match the PT consultative copy. Preserve every attribute, class, href, structure, and surrounding SVG checkmark markup. Only replace the text nodes.

    Line 65 — replace headline text:
    `<h1 class="hero-headline" data-i18n="hero.headline">Antes de automatizar, a gente entende onde seu negócio trava</h1>`

    Line 66 — replace subheadline text:
    `<p class="hero-subheadline" data-i18n="hero.subheadline">Fazemos o diagnóstico dos seus processos, identificamos gargalos e só então desenhamos a IA sob medida que seu negócio realmente precisa.</p>`

    Line 68 — replace primary CTA text (keep href, class, data-i18n, title, target, rel attributes exactly as-is):
    `<a href="https://wa.me/5521973495805" class="btn-cta-hero" data-i18n="hero.cta-primary" title="Agende seu Diagnóstico Gratuito" target="_blank" rel="noopener">Agende seu Diagnóstico Gratuito</a>`

    Line 69 — replace secondary CTA text (keep href="#how-it-works"):
    `<a href="#how-it-works" class="hero-cta-secondary" data-i18n="hero.cta-secondary">Veja como funciona &#8595;</a>`

    Line 72 — replace seal-1 inner text only (keep SVG checkmark and wrapping span):
    `<span data-i18n="hero.seal-1">Diagnóstico 100% gratuito</span>`

    Line 73 — replace seal-2 inner text only:
    `<span data-i18n="hero.seal-2">Sem compromisso</span>` (already matches — verify and keep)

    Line 74 — replace seal-3 inner text only:
    `<span data-i18n="hero.seal-3">Resposta imediata</span>` (already matches — verify and keep)

    DO NOT touch the `hero.badge` span (line 64) — out of scope for v2. DO NOT modify CSS classes, href values on the primary CTA (stays `https://wa.me/5521973495805`), or SVG paths.
  </action>
  <verify>
    <automated>grep -c "Antes de automatizar, a gente entende onde seu negócio trava" index.html &amp;&amp; grep -c "Fazemos o diagnóstico dos seus processos" index.html &amp;&amp; grep -c "Agende seu Diagnóstico Gratuito" index.html &amp;&amp; grep -c "Diagnóstico 100% gratuito" index.html &amp;&amp; grep -c 'href="#how-it-works"' index.html</automated>
  </verify>
  <acceptance_criteria>
    - `grep '<h1 class="hero-headline" data-i18n="hero.headline">Antes de automatizar, a gente entende onde seu negócio trava</h1>' index.html` returns 1 match
    - `grep 'data-i18n="hero.subheadline">Fazemos o diagnóstico dos seus processos, identificamos gargalos e só então desenhamos a IA sob medida que seu negócio realmente precisa.</p>' index.html` returns 1 match
    - `grep 'data-i18n="hero.cta-primary"' index.html | grep 'Agende seu Diagnóstico Gratuito'` returns at least 1 match
    - `grep 'href="https://wa.me/5521973495805" class="btn-cta-hero" data-i18n="hero.cta-primary"' index.html` returns 1 match (primary CTA URL preserved)
    - `grep 'href="#how-it-works" class="hero-cta-secondary" data-i18n="hero.cta-secondary">Veja como funciona &#8595;</a>' index.html` returns 1 match
    - `grep 'data-i18n="hero.seal-1">Diagnóstico 100% gratuito</span>' index.html` returns 1 match
    - `grep 'data-i18n="hero.seal-2">Sem compromisso</span>' index.html` returns 1 match
    - `grep 'data-i18n="hero.seal-3">Resposta imediata</span>' index.html` returns 1 match
    - OLD hero copy REMOVED: `grep "Seu negócio no piloto automático com Inteligência Artificial" index.html` returns 0 matches
    - OLD hero seal copy REMOVED: `grep 'data-i18n="hero.seal-1">Reunião 100% gratuita' index.html` returns 0 matches
    - `hero.badge` untouched: `grep 'data-i18n="hero.badge">&#10022; Agência de Automação com IA' index.html` returns 1 match
  </acceptance_criteria>
  <done>
    Hero section default markup text matches the new PT consultative copy for headline, subheadline, primary CTA, secondary CTA (anchored to #how-it-works), and 3 seals. Primary CTA still points to `https://wa.me/5521973495805`. `hero.badge` unchanged.
  </done>
</task>

</tasks>

<verification>
Open index.html in a browser (file:// or local server), verify:
1. Hero headline reads "Antes de automatizar, a gente entende onde seu negócio trava" on page load (PT default)
2. Sub-headline mentions "diagnóstico" and "gargalos"
3. Primary CTA button reads "Agende seu Diagnóstico Gratuito" and opens wa.me/5521973495805 in new tab
4. Secondary CTA "Veja como funciona ↓" scrolls smoothly to the "Simples assim" section
5. 3 seals show: "Diagnóstico 100% gratuito · Sem compromisso · Resposta imediata"
6. Clicking EN in language toggle updates all hero text to the English consultative copy
7. Clicking PT restores Portuguese
8. No console errors; `node --check script.js` exits 0
</verification>

<success_criteria>
- HERO-V2-01: Headline matches exact PT + EN copy in both index.html and script.js
- HERO-V2-02: Sub-headline matches exact PT + EN copy in both files
- HERO-V2-03: Primary CTA text "Agende seu Diagnóstico Gratuito" + href `https://wa.me/5521973495805`
- HERO-V2-04: Secondary CTA "Veja como funciona ↓" + href `#how-it-works`
- HERO-V2-05: 3 seals match "Diagnóstico 100% gratuito / Sem compromisso / Resposta imediata" in PT and EN equivalents
- Language toggle PT⇄EN swaps every hero text element
- No visual regression (no CSS touched); no JS parse errors
</success_criteria>

<output>
After completion, create `.planning/phases/08-hero-final-cta-rewrite/08-01-SUMMARY.md` following the summary template.
</output>
