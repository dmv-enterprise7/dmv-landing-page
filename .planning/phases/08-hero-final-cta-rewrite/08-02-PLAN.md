---
phase: 08-hero-final-cta-rewrite
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - index.html
  - script.js
autonomous: true
requirements:
  - CTA-V2-01
  - CTA-V2-02
  - CTA-V2-03

must_haves:
  truths:
    - "Visitor reaches final CTA section with title 'Pronto para descobrir onde seu negocio pode ir mais longe?' in PT"
    - "Visitor sees subtitle 'Comece com um diagnostico gratuito. Sem compromisso, sem enrolacao.' in PT"
    - "Visitor sees button 'Falar com um especialista agora' that opens https://wa.me/5521973495805"
    - "Visitor sees 3 final-CTA badges: 'Sem compromisso', '100% gratuito', 'Resposta imediata'"
    - "Switching language to EN updates every final-CTA element to the EN diagnostic copy"
  artifacts:
    - path: "index.html"
      provides: "Final CTA section markup with updated default text (fallback) — section id=\"cta-final\" preserved"
      contains: "id=\"cta-final\""
    - path: "script.js"
      provides: "translations.pt and translations.en cta.* keys updated to diagnostic-framed copy"
      contains: "cta.headline"
  key_links:
    - from: "index.html cta-final section data-i18n attributes"
      to: "script.js translations object"
      via: "setLanguage() lookup by key"
      pattern: "data-i18n=\"cta\\.(headline|subheadline|button|badge[1-3])\""
    - from: "index.html cta-final button"
      to: "WhatsApp diagnostic URL"
      via: "href attribute"
      pattern: "href=\"https://wa\\.me/5521973495805\""
---

<objective>
Rewrite the final CTA section (index.html markup + script.js cta.* PT/EN translations) so the closing conversion moment mirrors the hero's consultative "diagnosis first" language. No CSS changes. Preserve section id `cta-final`, classes, layout, animation, and button URL.

Purpose: Bookend the page — the final CTA is the last chance to convert. Today's copy ("Pronto para colocar seu negócio no piloto automático?") sells execution; the new copy ("Pronto para descobrir onde seu negócio pode ir mais longe?") frames the WhatsApp action as a diagnostic conversation. Must speak the same language as the rewritten hero (from Plan 08-01).

Output: Updated index.html final CTA section (lines ~462-482) and updated `cta.*` keys in script.js translations (both pt and en blocks). CTA-V2-01 through CTA-V2-03 satisfied.
</objective>

<execution_context>
@C:/Users/00amiri/Documents/DMV Ent/Projetos DMV/dmv-landing-page/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/00amiri/Documents/DMV Ent/Projetos DMV/dmv-landing-page/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-hero-final-cta-rewrite/08-CONTEXT.md
@.planning/phases/08-hero-final-cta-rewrite/08-01-SUMMARY.md

<interfaces>
<!-- Current final CTA section structure in index.html (lines 462-482) -->

```html
<section id="cta-final" class="section section-cta-final">
  ...
  <h2 class="cta-final-headline" data-i18n="cta.headline">...</h2>
  <p class="cta-final-sub" data-i18n="cta.subheadline">...</p>
  <a href="https://wa.me/5521973495805" class="btn-cta-final" data-i18n="cta.button" target="_blank" rel="noopener">...</a>
  ...
    <span data-i18n="cta.badge1">...</span>
    <span data-i18n="cta.badge2">...</span>
    <span data-i18n="cta.badge3">...</span>
</section>
```

<!-- Current i18n keys in script.js (cta block — both pt and en objects): -->
<!-- 'cta.headline', 'cta.subheadline', 'cta.button', 'cta.badge1', 'cta.badge2', 'cta.badge3' -->

<!-- Note: 'cta.button' text in EN already reads 'Talk to a specialist now' — can remain. PT currently reads 'Falar com um especialista agora' which matches the new copy exactly. Still validate both blocks explicitly. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update final CTA i18n keys in script.js (PT + EN)</name>
  <files>script.js</files>
  <read_first>
    - script.js (lines 111-122 and 225-232) — existing cta.* keys in both pt and en objects
  </read_first>
  <action>
    In script.js, update the cta.* keys inside BOTH `translations.pt` and `translations.en` objects. Preserve surrounding keys, comments, and commas.

    PT block (satisfies CTA-V2-01, CTA-V2-02, CTA-V2-03):
    - `'cta.headline'`: `'Pronto para descobrir onde seu negócio pode ir mais longe?'`
    - `'cta.subheadline'`: `'Comece com um diagnóstico gratuito. Sem compromisso, sem enrolação.'`
    - `'cta.button'`: `'Falar com um especialista agora'`  (unchanged value — still ensure it's present)
    - `'cta.badge1'`: `'Sem compromisso'`  (unchanged value)
    - `'cta.badge2'`: `'100% gratuito'`  (unchanged value)
    - `'cta.badge3'`: `'Resposta imediata'`  (unchanged value)

    EN block (mirror diagnostic tone):
    - `'cta.headline'`: `'Ready to discover how far your business can go?'`
    - `'cta.subheadline'`: `'Start with a free diagnosis. No commitment, no runaround.'`
    - `'cta.button'`: `'Talk to a specialist now'`  (unchanged value)
    - `'cta.badge1'`: `'No commitment'`  (unchanged value)
    - `'cta.badge2'`: `'100% free'`  (unchanged value)
    - `'cta.badge3'`: `'Immediate response'`  (unchanged value)

    Do not modify any other keys (navbar, hero, market, pain, services, how, diff, about, faq, footer).
  </action>
  <verify>
    <automated>grep -c "Pronto para descobrir onde seu negócio pode ir mais longe?" script.js &amp;&amp; grep -c "Ready to discover how far your business can go?" script.js &amp;&amp; grep -c "Comece com um diagnóstico gratuito. Sem compromisso, sem enrolação." script.js &amp;&amp; grep -c "Start with a free diagnosis. No commitment, no runaround." script.js &amp;&amp; node --check script.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep "'cta.headline': 'Pronto para descobrir onde seu negócio pode ir mais longe?'" script.js` returns 1 match
    - `grep "'cta.headline': 'Ready to discover how far your business can go?'" script.js` returns 1 match
    - `grep "'cta.subheadline': 'Comece com um diagnóstico gratuito. Sem compromisso, sem enrolação.'" script.js` returns 1 match
    - `grep "'cta.subheadline': 'Start with a free diagnosis. No commitment, no runaround.'" script.js` returns 1 match
    - `grep "'cta.button': 'Falar com um especialista agora'" script.js` returns 1 match
    - `grep "'cta.button': 'Talk to a specialist now'" script.js` returns 1 match
    - `grep "'cta.badge1': 'Sem compromisso'" script.js` returns 1 match
    - `grep "'cta.badge2': '100% gratuito'" script.js` returns 1 match
    - `grep "'cta.badge3': 'Resposta imediata'" script.js` returns 1 match
    - OLD copy REMOVED: `grep "Pronto para colocar seu negócio no piloto automático?" script.js` returns 0 matches
    - OLD copy REMOVED: `grep "Ready to put your business on autopilot?" script.js` returns 0 matches
    - OLD copy REMOVED: `grep "Comece com uma conversa gratuita" script.js` returns 0 matches
    - `node --check script.js` exits 0
  </acceptance_criteria>
  <done>
    `translations.pt.cta.headline` and `translations.pt.cta.subheadline` reflect new diagnostic copy; EN mirrors. button + 3 badges match exact strings (most unchanged). Script parses.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update final CTA markup default text in index.html</name>
  <files>index.html</files>
  <read_first>
    - index.html (lines 462-482) — current final CTA section markup
    - script.js (lines 111-122, 225-232) — canonical PT/EN cta copy from Task 1
  </read_first>
  <action>
    In index.html, update the default (fallback) text inside the final CTA section data-i18n elements to match the new PT copy. Preserve all classes, hrefs, attributes, and structure.

    Line 465 — replace headline text:
    `<h2 class="cta-final-headline" data-i18n="cta.headline">Pronto para descobrir onde seu negócio pode ir mais longe?</h2>`

    Line 466 — replace subheadline text:
    `<p class="cta-final-sub" data-i18n="cta.subheadline">Comece com um diagnóstico gratuito. Sem compromisso, sem enrolação.</p>`

    Line 467 — verify button (text should already match — keep href, class, data-i18n, target, rel exactly):
    `<a href="https://wa.me/5521973495805" class="btn-cta-final" data-i18n="cta.button" target="_blank" rel="noopener">Falar com um especialista agora</a>`

    Lines 471, 475, 479 — verify 3 badges already match. If any don't, set to:
    `<span data-i18n="cta.badge1">Sem compromisso</span>`
    `<span data-i18n="cta.badge2">100% gratuito</span>`
    `<span data-i18n="cta.badge3">Resposta imediata</span>`

    DO NOT modify the `section id="cta-final"` attribute, surrounding container divs, or any inline SVG/icon markup.
  </action>
  <verify>
    <automated>grep -c "Pronto para descobrir onde seu negócio pode ir mais longe?" index.html &amp;&amp; grep -c "Comece com um diagnóstico gratuito. Sem compromisso, sem enrolação." index.html &amp;&amp; grep -c 'href="https://wa.me/5521973495805" class="btn-cta-final"' index.html &amp;&amp; grep -c 'id="cta-final"' index.html</automated>
  </verify>
  <acceptance_criteria>
    - `grep '<h2 class="cta-final-headline" data-i18n="cta.headline">Pronto para descobrir onde seu negócio pode ir mais longe?</h2>' index.html` returns 1 match
    - `grep '<p class="cta-final-sub" data-i18n="cta.subheadline">Comece com um diagnóstico gratuito. Sem compromisso, sem enrolação.</p>' index.html` returns 1 match
    - `grep '<a href="https://wa.me/5521973495805" class="btn-cta-final" data-i18n="cta.button" target="_blank" rel="noopener">Falar com um especialista agora</a>' index.html` returns 1 match
    - `grep 'data-i18n="cta.badge1">Sem compromisso</span>' index.html` returns 1 match
    - `grep 'data-i18n="cta.badge2">100% gratuito</span>' index.html` returns 1 match
    - `grep 'data-i18n="cta.badge3">Resposta imediata</span>' index.html` returns 1 match
    - `grep '<section id="cta-final" class="section section-cta-final">' index.html` returns 1 match (section preserved)
    - OLD copy REMOVED: `grep "Pronto para colocar seu negócio no piloto automático?" index.html` returns 0 matches
    - OLD copy REMOVED: `grep "Comece com uma conversa gratuita" index.html` returns 0 matches
  </acceptance_criteria>
  <done>
    Final CTA section default markup matches new PT diagnostic copy for headline, subheadline, button (button unchanged but verified), and 3 badges. Section id and button href preserved.
  </done>
</task>

</tasks>

<verification>
Open index.html in browser, scroll to the final CTA section (end of page before footer). Verify:
1. Headline reads "Pronto para descobrir onde seu negócio pode ir mais longe?" in PT
2. Subtitle reads "Comece com um diagnóstico gratuito. Sem compromisso, sem enrolação."
3. Button reads "Falar com um especialista agora" and opens wa.me/5521973495805 in new tab
4. Badges: "Sem compromisso · 100% gratuito · Resposta imediata"
5. Toggle EN: all 5 elements update to English diagnostic copy
6. Toggle PT: restores Portuguese
7. Visual layout identical to v1 (no CSS regression)
8. `node --check script.js` exits 0
</verification>

<success_criteria>
- CTA-V2-01: `cta.headline` matches exact PT + EN copy in both index.html and script.js
- CTA-V2-02: `cta.subheadline` matches exact PT + EN copy; `cta.button` reads "Falar com um especialista agora" (PT) / "Talk to a specialist now" (EN) with href `https://wa.me/5521973495805`
- CTA-V2-03: 3 badges match "Sem compromisso · 100% gratuito · Resposta imediata" (PT) and EN equivalents
- No CSS modified; no JS parse errors; section id `cta-final` preserved
</success_criteria>

<output>
After completion, create `.planning/phases/08-hero-final-cta-rewrite/08-02-SUMMARY.md` following the summary template.
</output>
