---
phase: 09-provocacao-dados-mercado
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - index.html
  - script.js
autonomous: true
requirements: [MARK-V2-01, MARK-V2-02, MARK-V2-03]
must_haves:
  truths:
    - "Market data section heading reads 'O mercado já se moveu. E o seu negócio?' in PT"
    - "Market data section heading reads 'The market has already moved. What about your business?' in EN"
    - "Closing transition phrase now reads 'A pergunta não é SE você vai usar IA. É QUANDO. E quem começa com diagnóstico, começa certo.' (PT) and EN equivalent"
    - "All 6 statistic values and all 6 source citations remain byte-identical to pre-change state"
    - "Counter animation (IntersectionObserver) remains functional — numbers count up when section enters viewport"
    - "Section ID `#why-ai` preserved"
  artifacts:
    - path: "index.html"
      provides: "Updated default PT text for why-ai.title and why-ai.transition"
      contains: "O mercado já se moveu. E o seu negócio?"
    - path: "script.js"
      provides: "Updated PT + EN values for why-ai.title and why-ai.transition keys"
      contains: "A pergunta não é SE você vai usar IA. É QUANDO. E quem começa com diagnóstico, começa certo."
  key_links:
    - from: "index.html #why-ai"
      to: "script.js translations"
      via: "data-i18n keys why-ai.title / why-ai.transition"
      pattern: "data-i18n=\"why-ai\\.(title|transition)\""
    - from: "script.js counter animation"
      to: ".stat-number elements"
      via: "IntersectionObserver — PRESERVED"
      pattern: "IntersectionObserver"
---

<objective>
Reframe the Market Data ("Por que IA?") section title and closing transition phrase to align with the diagnosis-first positioning.

Purpose: Market statistics are already compelling; the new copy reframes them as an urgency signal for diagnosis rather than a generic AI-adoption pitch.
Output: index.html title default + script.js PT/EN values for `why-ai.title` and `why-ai.transition` only. All statistics, sources, subtitle, badge, CTA, and counter behavior preserved.
</objective>

<execution_context>
@C:/Users/00amiri/Documents/DMV Ent/Projetos DMV/dmv-landing-page/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/00amiri/Documents/DMV Ent/Projetos DMV/dmv-landing-page/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-provocacao-dados-mercado/09-CONTEXT.md
@index.html
@script.js

<interfaces>
Existing i18n keys to MODIFY (values only):
- why-ai.title
- why-ai.transition

Existing i18n keys to PRESERVE EXACTLY (DO NOT touch):
- why-ai.badge
- why-ai.subtitle
- why-ai.stat1.desc / .source through why-ai.stat6.desc / .source (12 keys)
- why-ai.cta

Existing HTML structure (index.html lines ~86-132):
- `<section id="why-ai">` — ID PRESERVED
- `<h2 class="why-ai-title" data-i18n="why-ai.title">` — UPDATE
- `<p class="transition-phrase" data-i18n="why-ai.transition">` — UPDATE
- 6× stat blocks (`.stat-number`, `.stat-desc`, `.stat-source`) — PRESERVE

NOTE: Current script.js line 33 already shows `'why-ai.title': 'O mercado já se moveu. E o seu negócio?'` — verify current state; if already matching CONTEXT decision exactly, mark the PT title task as no-op. The EN title currently reads "The market has already moved. Has your business?" which must change to "The market has already moved. What about your business?" per CONTEXT. The PT transition must change. The EN transition must change.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update why-ai.title (EN if needed) and why-ai.transition (PT + EN) in index.html default + script.js translations</name>
  <read_first>
    - index.html (lines 85-135 — why-ai section)
    - script.js (lines 28-55 — PT why-ai block; lines 145-165 — EN why-ai block)
  </read_first>
  <files>index.html, script.js</files>
  <action>
STEP A — index.html default text inside `<section id="why-ai">` (do NOT rename ID):

1. Verify `<h2 class="why-ai-title" data-i18n="why-ai.title">` default text is exactly: `O mercado já se moveu. E o seu negócio?` — if already that, leave; if different, update.

2. Replace `<p class="transition-phrase" data-i18n="why-ai.transition">` text with EXACTLY:
   `A pergunta não é SE você vai usar IA. É QUANDO. E quem começa com diagnóstico, começa certo.`

DO NOT modify: the `.section-badge`, `.why-ai-subtitle`, any `.stat-number`, `.stat-desc`, `.stat-source`, or `.btn-cta-market`.

STEP B — script.js PT block (around lines 32-48):

1. Ensure `'why-ai.title': 'O mercado já se moveu. E o seu negócio?',` (likely already correct — no-op if so).

2. Replace `'why-ai.transition'` value with EXACTLY:
   `'why-ai.transition': 'A pergunta não é SE você vai usar IA. É QUANDO. E quem começa com diagnóstico, começa certo.',`

STEP C — script.js EN block (around lines 146-162):

1. Replace `'why-ai.title'` value with EXACTLY:
   `'why-ai.title': 'The market has already moved. What about your business?',`
   (Current value "Has your business?" must become "What about your business?" per CONTEXT decision — MARK-V2-01 EN.)

2. Replace `'why-ai.transition'` value with EXACTLY:
   `'why-ai.transition': 'The question isn\'t IF you\'ll use AI. It\'s WHEN. And those who start with diagnosis start right.',`

DO NOT touch any `why-ai.stat*`, `why-ai.badge`, `why-ai.subtitle`, or `why-ai.cta` keys — MARK-V2-03 requires all statistics and sources preserved.

Implements MARK-V2-01 (title), MARK-V2-02 (closing), MARK-V2-03 (preserve statistics + sources).
  </action>
  <acceptance_criteria>
    - `grep -c 'id="why-ai"' index.html` returns at least `1` (section preserved; `#why-ai` also in nav/footer links)
    - `grep -F "O mercado já se moveu. E o seu negócio?" index.html` finds at least one match (h2 default)
    - `grep -F "A pergunta não é SE você vai usar IA. É QUANDO. E quem começa com diagnóstico, começa certo." index.html` finds the match
    - `grep -F "A pergunta não é SE você vai usar IA. É QUANDO. E quem começa antes, sai na frente." index.html` returns zero matches (old PT transition removed)
    - `grep -F "'why-ai.title': 'O mercado já se moveu. E o seu negócio?'" script.js` finds the match (PT)
    - `grep -F "'why-ai.title': 'The market has already moved. What about your business?'" script.js` finds the match (EN)
    - `grep -F "'why-ai.title': 'The market has already moved. Has your business?'" script.js` returns zero matches (old EN removed)
    - `grep -F "A pergunta não é SE você vai usar IA. É QUANDO. E quem começa com diagnóstico, começa certo." script.js` finds the match (PT transition)
    - `grep -F "The question isn" script.js &amp;&amp; grep -F "those who start with diagnosis start right." script.js` finds EN transition
    - `grep -F "A pergunta não é SE você vai usar IA. É QUANDO. E quem começa antes, sai na frente." script.js` returns zero matches (old PT transition gone)
    - `grep -F "those who start first, lead the race." script.js` returns zero matches (old EN transition gone)
    - **Preservation (MARK-V2-03 — statistics + sources byte-identical):**
      - `grep -F "McKinsey, State of AI 2025" script.js | wc -l` returns `2` (PT + EN)
      - `grep -F "IBM Research" script.js | wc -l` returns `2`
      - `grep -F "Tidio Research" script.js | wc -l` returns `2`
      - `grep -F "Zendesk / Flowlyn" script.js | wc -l` returns `2`
      - `grep -F "MDPI Academic Study, 2025" script.js | wc -l` returns `2`
      - `grep -F "Sales So / Industry Data" script.js | wc -l` returns `2`
      - `grep -c "why-ai.stat" script.js` returns `24` (6 stats × 2 fields × 2 languages)
    - **Preservation (counter animation):** `grep -c "IntersectionObserver" script.js` unchanged (>=1); `grep -c "class=\"stat-number\"" index.html` returns `6`
    - `node --check script.js` exits 0
  </acceptance_criteria>
  <verify>
    <automated>node --check script.js &amp;&amp; grep -F "A pergunta não é SE você vai usar IA. É QUANDO. E quem começa com diagnóstico, começa certo." index.html &amp;&amp; grep -F "'why-ai.title': 'The market has already moved. What about your business?'" script.js &amp;&amp; grep -F "those who start with diagnosis start right." script.js &amp;&amp; ! grep -qF "E quem começa antes, sai na frente." script.js &amp;&amp; [ "$(grep -c 'why-ai.stat' script.js)" = "24" ]</automated>
  </verify>
  <done>why-ai.title (EN) and why-ai.transition (PT + EN) reflect CONTEXT canonical copy; index.html h2 + transition-phrase match; all 6 statistic keys × 2 languages + 6 sources preserved; counter animation code untouched; `node --check` passes.</done>
</task>

</tasks>

<verification>
- Open index.html in browser, scroll to #why-ai section → title shows new PT; toggle EN → new EN title appears
- Scroll-trigger counter animation still runs (numbers count from 0 to final values)
- All 6 stat cards still display their numbers + descriptions + sources unchanged
- Closing transition phrase below data cards shows the new diagnosis-framed text in both languages
- Market CTA button below transition still links to WhatsApp (untouched)
</verification>

<success_criteria>
- MARK-V2-01: Title matches CONTEXT exactly in PT + EN
- MARK-V2-02: Closing phrase reframed in PT + EN per CONTEXT
- MARK-V2-03: All 6 statistics (78/4.8/62/70/46/67) and all 6 sources (McKinsey/IBM/Tidio/Zendesk/MDPI/Sales So) unchanged
- Counter animation behavior preserved (IntersectionObserver code untouched)
- Section ID `#why-ai` preserved; subtitle, badge, CTA untouched
- `node --check script.js` passes
</success_criteria>

<output>
After completion, create `.planning/phases/09-provocacao-dados-mercado/09-02-SUMMARY.md` documenting the exact strings changed and confirming preservation of statistics + counter.
</output>
