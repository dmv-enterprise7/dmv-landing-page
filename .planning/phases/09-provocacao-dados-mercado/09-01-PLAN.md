---
phase: 09-provocacao-dados-mercado
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
  - script.js
autonomous: true
requirements: [PROV-01, PROV-02, PROV-03]
must_haves:
  truths:
    - "Section heading reads 'Você sabe onde está perdendo dinheiro?' in PT"
    - "Section heading reads 'Do you know where you're losing money?' in EN"
    - "4 provocation cards display the exact diagnostic questions (leads, manual steps, time on AI-resolvable tasks, shift-dependent quality)"
    - "Closing line reframes the section around diagnosis ('Nosso trabalho começa exatamente aí.')"
    - "Section ID `#pain-points` is preserved (internal anchors must not break)"
    - "Language toggle swaps every text node to matching EN string"
  artifacts:
    - path: "index.html"
      provides: "Updated PT default text inside #pain-points section"
      contains: "Você sabe onde está perdendo dinheiro?"
    - path: "script.js"
      provides: "Updated PT + EN translation values for pain.* keys"
      contains: "Quantos leads esfriaram porque ninguém respondeu em 5 minutos?"
  key_links:
    - from: "index.html #pain-points"
      to: "script.js translations"
      via: "data-i18n keys pain.title / pain.item1-4 / pain.transition"
      pattern: "data-i18n=\"pain\\."
---

<objective>
Rewrite the former "Isso é familiar?" (Pain Points) section into the new "Provocação" diagnostic-questions section.

Purpose: Convert pain statements into provocative questions that force the reader to realize they don't know the answers, priming them for the diagnosis offer.
Output: Updated index.html default PT text + script.js PT/EN translations for all 5 pain.* i18n keys. No CSS, no structural/layout changes, section ID preserved.
</objective>

<execution_context>
@C:/Users/00amiri/Documents/DMV Ent/Projetos DMV/dmv-landing-page/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/00amiri/Documents/DMV Ent/Projetos DMV/dmv-landing-page/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-provocacao-dados-mercado/09-CONTEXT.md
@index.html
@script.js

<interfaces>
Existing i18n keys (DO NOT rename — update values only):
- pain.title
- pain.item1
- pain.item2
- pain.item3
- pain.item4
- pain.transition

Existing HTML structure (index.html lines ~137-172):
- `<section id="pain-points" class="section section-pain-points">` — ID PRESERVED
- `<h2 data-i18n="pain.title">`
- 4× `<p class="pain-text" data-i18n="pain.itemN">`
- `<p class="pain-transition-text" data-i18n="pain.transition">`

Translations object (script.js):
- PT block starts at line ~49 (pt.pain.*)
- EN block starts at line ~163 (en.pain.*)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update default PT copy in index.html #pain-points section</name>
  <read_first>index.html (lines 130-180 — pain-points section)</read_first>
  <files>index.html</files>
  <action>
In index.html within `<section id="pain-points">` (do NOT rename the ID), replace ONLY the text content of the following elements (keep all class names, data-i18n attributes, SVG icons, and structure intact):

1. `<h2 data-i18n="pain.title">` → text: `Você sabe onde está perdendo dinheiro?`

2. `<p class="pain-text" data-i18n="pain.item1">` → text: `Quantos leads esfriaram porque ninguém respondeu em 5 minutos?`

3. `<p class="pain-text" data-i18n="pain.item2">` → text: `Você sabe quantas etapas manuais seu time repete todo dia sem necessidade?`

4. `<p class="pain-text" data-i18n="pain.item3">` → text: `Já mediu quanto tempo sua equipe gasta em tarefas que uma IA resolve em segundos?`

5. `<p class="pain-text" data-i18n="pain.item4">` → text: `Seu atendimento muda de qualidade dependendo de quem está no plantão?`

6. `<p class="pain-transition-text" data-i18n="pain.transition">` → text: `A maioria das empresas não sabe responder essas perguntas. Nosso trabalho começa exatamente aí.`

Implements PROV-01, PROV-02, PROV-03.
DO NOT touch the SVG icons, classnames, grid container `.pain-points-grid`, or section id `pain-points`.
  </action>
  <acceptance_criteria>
    - `grep -c 'id="pain-points"' index.html` returns `1` (section ID preserved)
    - `grep -F 'Você sabe onde está perdendo dinheiro?' index.html` finds the match
    - `grep -F 'Quantos leads esfriaram porque ninguém respondeu em 5 minutos?' index.html` finds the match
    - `grep -F 'Você sabe quantas etapas manuais seu time repete todo dia sem necessidade?' index.html` finds the match
    - `grep -F 'Já mediu quanto tempo sua equipe gasta em tarefas que uma IA resolve em segundos?' index.html` finds the match
    - `grep -F 'Seu atendimento muda de qualidade dependendo de quem está no plantão?' index.html` finds the match
    - `grep -F 'A maioria das empresas não sabe responder essas perguntas. Nosso trabalho começa exatamente aí.' index.html` finds the match
    - `grep -F 'Isso é familiar?' index.html` returns zero matches (old title removed)
    - `grep -F 'Perde clientes porque demora para responder' index.html` returns zero matches (old item1 removed)
    - `grep -F 'A DMV resolve cada um desses problemas com Inteligência Artificial.' index.html` returns zero matches (old transition removed)
    - `grep -c 'data-i18n="pain.item' index.html` returns `4`
  </acceptance_criteria>
  <verify>
    <automated>grep -F "Você sabe onde está perdendo dinheiro?" index.html &amp;&amp; grep -c 'id="pain-points"' index.html | grep -q '^1$' &amp;&amp; ! grep -qF "Isso é familiar?" index.html</automated>
  </verify>
  <done>All 6 PT defaults inside #pain-points match CONTEXT.md canonical strings; section ID unchanged; zero legacy pain strings remain.</done>
</task>

<task type="auto">
  <name>Task 2: Update PT + EN translations for pain.* keys in script.js</name>
  <read_first>script.js (lines 1-200 — translations object PT then EN blocks)</read_first>
  <files>script.js</files>
  <action>
In script.js, locate the `translations` object and update the VALUES of the existing pain.* keys in both the PT (`pt`) and EN (`en`) blocks. DO NOT rename any key. DO NOT add new keys.

PT block (around line 49-54) — new values:
- `'pain.title': 'Você sabe onde está perdendo dinheiro?',`
- `'pain.item1': 'Quantos leads esfriaram porque ninguém respondeu em 5 minutos?',`
- `'pain.item2': 'Você sabe quantas etapas manuais seu time repete todo dia sem necessidade?',`
- `'pain.item3': 'Já mediu quanto tempo sua equipe gasta em tarefas que uma IA resolve em segundos?',`
- `'pain.item4': 'Seu atendimento muda de qualidade dependendo de quem está no plantão?',`
- `'pain.transition': 'A maioria das empresas não sabe responder essas perguntas. Nosso trabalho começa exatamente aí.',`

EN block (around line 163-168) — new values:
- `'pain.title': 'Do you know where you\'re losing money?',`
- `'pain.item1': 'How many leads went cold because no one responded within 5 minutes?',`
- `'pain.item2': 'Do you know how many manual steps your team repeats every day without need?',`
- `'pain.item3': 'Have you measured how much time your team spends on tasks AI solves in seconds?',`
- `'pain.item4': 'Does your service quality change depending on who\'s on shift?',`
- `'pain.transition': 'Most businesses can\'t answer these questions. That\'s exactly where our work begins.',`

Escape single quotes inside English strings with backslash (you're → you\'re, who's → who\'s, can't → can\'t, That's → That\'s) to match the existing file style.

Implements PROV-01 (title), PROV-02 (4 cards), PROV-03 (closing) for both languages.
  </action>
  <acceptance_criteria>
    - `grep -F "'pain.title': 'Você sabe onde está perdendo dinheiro?'" script.js` finds the match (PT)
    - `grep -F "'pain.item1': 'Quantos leads esfriaram porque ninguém respondeu em 5 minutos?'" script.js` finds the match
    - `grep -F "'pain.item2': 'Você sabe quantas etapas manuais seu time repete todo dia sem necessidade?'" script.js` finds the match
    - `grep -F "'pain.item3': 'Já mediu quanto tempo sua equipe gasta em tarefas que uma IA resolve em segundos?'" script.js` finds the match
    - `grep -F "'pain.item4': 'Seu atendimento muda de qualidade dependendo de quem está no plantão?'" script.js` finds the match
    - `grep -F "A maioria das empresas não sabe responder essas perguntas. Nosso trabalho começa exatamente aí." script.js` finds the match
    - `grep -F "Do you know where you" script.js` finds EN title
    - `grep -F "How many leads went cold because no one responded within 5 minutes?" script.js` finds the match
    - `grep -F "Most businesses can" script.js &amp;&amp; grep -F "That's exactly where our work begins." script.js` finds EN closing (allowing escaped apostrophes)
    - `grep -F "Perde clientes porque demora para responder" script.js` returns zero matches
    - `grep -F "Isso é familiar?" script.js` returns zero matches
    - `grep -F "A DMV resolve cada um desses problemas com Inteligência Artificial." script.js` returns zero matches
    - `grep -F "Losing customers because you take too long to respond" script.js` returns zero matches
    - `grep -F "DMV solves every one of these problems with Artificial Intelligence." script.js` returns zero matches
    - `grep -c "'pain\." script.js` returns `12` (6 keys × 2 languages)
    - `node -e "require('./script.js')"` — skip; file uses DOMContentLoaded, just ensure no syntax error by `node --check script.js`
  </acceptance_criteria>
  <verify>
    <automated>node --check script.js &amp;&amp; grep -F "Você sabe onde está perdendo dinheiro?" script.js &amp;&amp; grep -F "How many leads went cold because no one responded within 5 minutes?" script.js &amp;&amp; ! grep -qF "Isso é familiar?" script.js</automated>
  </verify>
  <done>All 6 pain.* keys in both PT and EN blocks hold the new diagnostic-question copy; script.js passes `node --check`; no legacy pain strings remain.</done>
</task>

</tasks>

<verification>
- Open index.html in browser, scroll to former pain section → observe new title and 4 questions in PT
- Toggle language EN → all 6 strings swap to English equivalents
- Internal anchor `#pain-points` still navigates to section
- Counter animation / other sections unaffected
</verification>

<success_criteria>
- Section displays "Você sabe onde está perdendo dinheiro?" in PT, "Do you know where you're losing money?" in EN
- 4 provocation cards show the exact diagnostic questions (PT + EN) from CONTEXT.md
- Closing line in both languages frames the diagnosis offer
- Section ID preserved; layout unchanged; `node --check script.js` passes
- No legacy "Isso é familiar?" / "Does this sound familiar?" strings remain anywhere
</success_criteria>

<output>
After completion, create `.planning/phases/09-provocacao-dados-mercado/09-01-SUMMARY.md` documenting the exact strings changed and files touched.
</output>
