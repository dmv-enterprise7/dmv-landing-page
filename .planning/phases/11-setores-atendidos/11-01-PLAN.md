---
phase: 11-setores-atendidos
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
  - script.js
autonomous: true
requirements: [SECT-01, SECT-02, SECT-03, SECT-04, SECT-07]
must_haves:
  truths:
    - "New #sectors section exists in the DOM between #services and #differentials"
    - "Section renders 6 sector cards with SVG icon + label each"
    - "Section title, 6 labels, and closing line all have data-i18n keys"
    - "Navbar (desktop + mobile) and footer include a link to #sectors"
    - "Language toggle updates every text element in the new section in both PT and EN"
  artifacts:
    - path: "index.html"
      provides: "Sectors section markup + nav + footer entries"
      contains: "id=\"sectors\""
    - path: "script.js"
      provides: "i18n keys for sectors.title, sectors.closing, sectors.s1-6.label, nav.sectors in PT and EN"
      contains: "sectors.title"
  key_links:
    - from: "index.html navbar a[href='#sectors']"
      to: "section#sectors"
      via: "anchor link"
      pattern: "href=\"#sectors\""
    - from: "index.html footer-nav a[href='#sectors']"
      to: "section#sectors"
      via: "anchor link"
      pattern: "href=\"#sectors\""
    - from: "index.html [data-i18n='sectors.*']"
      to: "translations.pt/en['sectors.*']"
      via: "setLanguage() in script.js"
      pattern: "data-i18n=\"sectors\\."
---

<objective>
Add the new "Setores que já transformamos com IA" section to the DOM between #services and #differentials, wire it into the navbar (desktop + mobile drawer) and footer navigation, and register all required PT/EN i18n keys in script.js so the language toggle updates every text element in the new section.

Purpose: Structural content addition for v2.0 repositioning — visitors see the 6 sectors DMV serves and can navigate to the section from both nav and footer.
Output: New section markup in index.html, new nav/footer anchors, new i18n keys in script.js. CSS styling is NOT in scope here — plan 11-02 handles visual styling.
</objective>

<execution_context>
@C:/Users/00amiri/Documents/DMV Ent/Projetos DMV/dmv-landing-page/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/00amiri/Documents/DMV Ent/Projetos DMV/dmv-landing-page/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-setores-atendidos/11-CONTEXT.md
@index.html
@script.js

<interfaces>
<!-- Existing patterns to follow — extracted from the codebase. Use these directly. -->

Existing nav entries in index.html (lines 35-41):
```html
<div class="nav-links" id="nav-links">
  <a href="#why-ai" data-i18n="nav.why">Por que IA?</a>
  <a href="#services" data-i18n="nav.services">Serviços</a>
  <a href="#how-it-works" data-i18n="nav.how">Como Funciona</a>
  <a href="#about" data-i18n="nav.about">Sobre Nós</a>
  <a href="#faq" data-i18n="nav.faq">FAQ</a>
</div>
```

Existing footer nav in index.html (lines 512-518):
```html
<nav class="footer-nav" aria-label="Footer navigation">
  <a href="#why-ai" data-i18n="nav.why">Por que IA?</a>
  <a href="#services" data-i18n="nav.services">Serviços</a>
  <a href="#how-it-works" data-i18n="nav.how">Como Funciona</a>
  <a href="#about" data-i18n="nav.about">Sobre Nós</a>
  <a href="#faq" data-i18n="nav.faq">FAQ</a>
</nav>
```

Existing section pattern (e.g. #differentials line 310):
```html
<section id="differentials" class="section section-differentials">
  <div class="container">
    <div class="diff-header">
      <h2 data-i18n="diff.title">...</h2>
      <p class="diff-subtitle" data-i18n="diff.subtitle">...</p>
    </div>
    <div class="diff-grid">
      <div class="diff-card">
        <div class="diff-icon"><svg ...></svg></div>
        <div class="diff-body">
          <h3 class="diff-title" data-i18n="diff.item1.title">...</h3>
          <p class="diff-desc" data-i18n="diff.item1.desc">...</p>
        </div>
      </div>
      ...
    </div>
  </div>
</section>
```

i18n system (script.js lines 8, 246-266):
```js
var translations = { pt: { 'key': 'value', ... }, en: { ... } };
function setLanguage(lang) {
  document.querySelectorAll('[data-i18n]').forEach(function(el) {
    var key = el.getAttribute('data-i18n');
    if (el.getAttribute('data-i18n-number') === 'true') return;
    if (translations[lang] && translations[lang][key]) el.textContent = translations[lang][key];
  });
  ...
}
```

SVG icon style (stroke, no fill) — reference existing diff-icon at lines 321-325:
```html
<svg width="28" height="28" viewBox="0 0 28 28" fill="none" stroke="currentColor"
     stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
  <!-- paths -->
</svg>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Insert #sectors section, nav link, footer link in index.html</name>
  <read_first>
    - index.html lines 35-41 (desktop nav-links block)
    - index.html lines 177-232 (#services section — this ends at line 232; new section goes immediately after)
    - index.html lines 310-345 (#differentials — pattern reference for glassmorphism + icon-card markup)
    - index.html lines 512-518 (footer-nav)
  </read_first>
  <files>index.html</files>
  <action>
Make three edits to index.html:

**Edit A — Desktop nav links (add one anchor after `#services`, before `#how-it-works`)**

In the `<div class="nav-links" id="nav-links">` block (lines 35-41), insert a new anchor AFTER the Serviços link and BEFORE the Como Funciona link:

```html
<a href="#sectors" data-i18n="nav.sectors">Setores</a>
```

Resulting order: Por que IA? | Serviços | **Setores** | Como Funciona | Sobre Nós | FAQ.

**Edit B — Insert new section between `#services` and `#how-it-works`**

`#services` section closes at the line containing `</section>` immediately before `<section id="how-it-works"` (around line 232-234). Insert the following block between `</section>` (end of services) and `<section id="how-it-works"` (start of how-it-works):

```html
    <section id="sectors" class="section section-sectors">
      <div class="container">
        <div class="sectors-header">
          <h2 data-i18n="sectors.title">Setores que já transformamos com IA</h2>
        </div>

        <div class="sectors-grid">

          <div class="sector-card">
            <div class="sector-icon">
              <svg width="28" height="28" viewBox="0 0 28 28" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
                <path d="M14 3v22"/>
                <path d="M3 14h22"/>
                <rect x="7" y="7" width="14" height="14" rx="2"/>
              </svg>
            </div>
            <h3 class="sector-label" data-i18n="sectors.s1.label">Clínicas e Consultórios</h3>
          </div>

          <div class="sector-card">
            <div class="sector-icon">
              <svg width="28" height="28" viewBox="0 0 28 28" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
                <path d="M5 8h18l-2 13H7L5 8z"/>
                <path d="M9 8V5a5 5 0 0110 0v3"/>
              </svg>
            </div>
            <h3 class="sector-label" data-i18n="sectors.s2.label">E-commerces</h3>
          </div>

          <div class="sector-card">
            <div class="sector-icon">
              <svg width="28" height="28" viewBox="0 0 28 28" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
                <rect x="3" y="8" width="22" height="15" rx="2"/>
                <path d="M10 8V5a2 2 0 012-2h4a2 2 0 012 2v3"/>
                <path d="M3 15h22"/>
              </svg>
            </div>
            <h3 class="sector-label" data-i18n="sectors.s3.label">Escritórios e Consultorias</h3>
          </div>

          <div class="sector-card">
            <div class="sector-icon">
              <svg width="28" height="28" viewBox="0 0 28 28" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
                <path d="M20.4 10.6a5 5 0 01-6.8 6.8l-8 8-3-3 8-8a5 5 0 016.8-6.8l-3 3 2 2 3-3z"/>
              </svg>
            </div>
            <h3 class="sector-label" data-i18n="sectors.s4.label">Prestadores de Serviço</h3>
          </div>

          <div class="sector-card">
            <div class="sector-icon">
              <svg width="28" height="28" viewBox="0 0 28 28" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
                <path d="M14 4L2 10l12 6 12-6-12-6z"/>
                <path d="M6 13v6c0 1.5 4 3 8 3s8-1.5 8-3v-6"/>
              </svg>
            </div>
            <h3 class="sector-label" data-i18n="sectors.s5.label">Educação e Cursos</h3>
          </div>

          <div class="sector-card">
            <div class="sector-icon">
              <svg width="28" height="28" viewBox="0 0 28 28" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
                <path d="M4 12l10-8 10 8v12H4V12z"/>
                <path d="M11 24v-7h6v7"/>
              </svg>
            </div>
            <h3 class="sector-label" data-i18n="sectors.s6.label">Imobiliárias</h3>
          </div>

        </div>

        <p class="sectors-closing" data-i18n="sectors.closing">Se o seu negócio atende clientes, a IA pode ajudar. Agende o diagnóstico e descubra como.</p>
      </div>
    </section>

```

(Indentation uses 4 spaces to match the surrounding `<main>` children; preserve a single blank line before `<section id="how-it-works"`.)

**Edit C — Footer nav (add anchor after `#services`, before `#how-it-works`)**

In the `<nav class="footer-nav">` block (lines 512-518), insert AFTER the Serviços link and BEFORE the Como Funciona link:

```html
<a href="#sectors" data-i18n="nav.sectors">Setores</a>
```

Do NOT touch CSS (plan 11-02). Do NOT change translations yet (Task 2).
  </action>
  <verify>
    <automated>grep -c "id=\"sectors\"" index.html | grep -q "^1$" && grep -c "href=\"#sectors\"" index.html | grep -q "^2$" && grep -c "data-i18n=\"sectors.s" index.html | grep -q "^6$" && grep -q "data-i18n=\"sectors.title\"" index.html && grep -q "data-i18n=\"sectors.closing\"" index.html && grep -q "data-i18n=\"nav.sectors\"" index.html</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'id="sectors"' index.html` returns exactly 1 line
    - `grep -c 'href="#sectors"' index.html` returns exactly 2 (navbar + footer)
    - `grep -c 'data-i18n="sectors.s[1-6].label"' index.html` returns exactly 6
    - `grep -q 'data-i18n="sectors.title"' index.html` matches
    - `grep -q 'data-i18n="sectors.closing"' index.html` matches
    - `grep -q 'data-i18n="nav.sectors"' index.html` matches (appears twice — nav + footer)
    - `grep -n 'id="services"\|id="sectors"\|id="how-it-works"' index.html` shows order: services, sectors, how-it-works
    - The 6 default PT labels literally appear in the file: "Clínicas e Consultórios", "E-commerces", "Escritórios e Consultorias", "Prestadores de Serviço", "Educação e Cursos", "Imobiliárias"
    - The PT closing sentence literally appears: "Se o seu negócio atende clientes, a IA pode ajudar. Agende o diagnóstico e descubra como."
  </acceptance_criteria>
  <done>HTML section exists with correct ID, 6 cards with icons and labels, closing line, and both nav + footer links point to #sectors. Fulfills SECT-01, SECT-02, SECT-03, SECT-04.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add sectors + nav.sectors i18n keys to translations (PT + EN) in script.js</name>
  <read_first>
    - script.js lines 8-124 (PT translations block — find a logical insertion point between services and how blocks OR between services and diff)
    - script.js lines 125-240 (EN translations block — mirror insertion point)
  </read_first>
  <files>script.js</files>
  <action>
Add 8 new keys per language (total 16 entries) to the `translations` object.

**Keys to add (identical structure in PT and EN):**
- `nav.sectors`
- `sectors.title`
- `sectors.s1.label`
- `sectors.s2.label`
- `sectors.s3.label`
- `sectors.s4.label`
- `sectors.s5.label`
- `sectors.s6.label`
- `sectors.closing`

**Insertion location (PT block):** Add `nav.sectors` next to other `nav.*` keys (after `'nav.faq'` line, before `'nav.cta'`). Add the `sectors.*` block AFTER `services.card6.desc` and BEFORE `how.title` (logical content order: services → sectors → how).

**Insertion location (EN block):** Mirror the PT placement.

**PT values (exact strings, preserve accents):**
```js
// nav block
'nav.sectors': 'Setores',

// sectors block (insert after services.card6.desc, before how.title)
'sectors.title': 'Setores que já transformamos com IA',
'sectors.s1.label': 'Clínicas e Consultórios',
'sectors.s2.label': 'E-commerces',
'sectors.s3.label': 'Escritórios e Consultorias',
'sectors.s4.label': 'Prestadores de Serviço',
'sectors.s5.label': 'Educação e Cursos',
'sectors.s6.label': 'Imobiliárias',
'sectors.closing': 'Se o seu negócio atende clientes, a IA pode ajudar. Agende o diagnóstico e descubra como.',
```

**EN values (exact strings):**
```js
// nav block
'nav.sectors': 'Sectors',

// sectors block
'sectors.title': 'Sectors we\u0027ve already transformed with AI',
'sectors.s1.label': 'Clinics & Medical Offices',
'sectors.s2.label': 'E-commerces',
'sectors.s3.label': 'Offices & Consulting Firms',
'sectors.s4.label': 'Service Providers',
'sectors.s5.label': 'Education & Courses',
'sectors.s6.label': 'Real Estate',
'sectors.closing': 'If your business serves clients, AI can help. Schedule your diagnosis and find out how.',
```

(The apostrophe in the EN title is escaped as `\u0027` to match existing style patterns used elsewhere in script.js for apostrophes inside single-quoted strings. Alternatively you may use a straight `'` with the entire value wrapped in double quotes — both are acceptable. Do NOT use a curly apostrophe.)

Preserve trailing commas consistent with surrounding entries. Do NOT touch any other keys.
  </action>
  <verify>
    <automated>node -e "var t=require('./script.js'); " 2>/dev/null; node -e "var fs=require('fs');var s=fs.readFileSync('script.js','utf8');var ptMatches=(s.match(/'sectors\.[a-z0-9.]+'\s*:/g)||[]).length;var navMatches=(s.match(/'nav\.sectors'\s*:/g)||[]).length;if(ptMatches!==16){console.error('sectors.* expected 16 (8 PT + 8 EN), got '+ptMatches);process.exit(1);}if(navMatches!==2){console.error('nav.sectors expected 2 (PT + EN), got '+navMatches);process.exit(1);}console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "'sectors\.title'" script.js` returns exactly 2 (PT + EN)
    - `grep -c "'sectors\.s[1-6]\.label'" script.js` returns exactly 12 (6 labels × 2 languages)
    - `grep -c "'sectors\.closing'" script.js` returns exactly 2
    - `grep -c "'nav\.sectors'" script.js` returns exactly 2
    - PT string literals present in file: `'Setores que já transformamos com IA'`, `'Clínicas e Consultórios'`, `'Imobiliárias'`
    - EN string literals present in file: `'Sectors'`, `'Clinics & Medical Offices'`, `'Real Estate'`
    - `node --check script.js` passes (no syntax errors — trailing commas and quotes valid)
    - Opening the page and clicking the EN toggle updates the section title to "Sectors we've already transformed with AI" and all 6 labels to their EN values (smoke-verifiable in browser; automated grep covers static presence)
  </acceptance_criteria>
  <done>Both PT and EN translation blocks contain all 9 new keys (nav.sectors + 8 sectors.* keys). script.js parses without syntax errors. Fulfills SECT-07.</done>
</task>

</tasks>

<verification>
Run all acceptance grep checks from both tasks. Additionally:
- `node --check script.js` passes
- Open index.html in browser: new section is visible between Soluções and Diferenciais (unstyled grid is acceptable at this point — 11-02 adds CSS); clicking a nav "Setores" link scrolls smoothly to the section; language toggle updates all new text elements.
</verification>

<success_criteria>
- All 5 requirements addressed: SECT-01 (section exists between services and differentials), SECT-02 (6 sector cards with icons + labels), SECT-03 (closing line), SECT-04 (nav + footer links), SECT-07 (full PT/EN i18n)
- Zero JavaScript errors in console
- `grep 'id="sectors"\|id="services"\|id="how-it-works"' index.html` shows them in correct DOM order
</success_criteria>

<output>
After completion, create `.planning/phases/11-setores-atendidos/11-01-SUMMARY.md` documenting:
- Files modified (index.html, script.js) with line count delta
- Exact section ID chosen (`sectors`) for downstream plans
- i18n keys added (list of 9 keys)
- Nav/footer placement (between Serviços and Como Funciona)
- Any icon SVG paths adjusted from the plan (for 11-02 to reference)
</output>
