---
phase: 12-diferenciais-quem-somos-faq
plan: 02
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - index.html
  - script.js
  - styles.css
autonomous: true
requirements:
  - ABOUT-V2-01
  - ABOUT-V2-02
  - ABOUT-V2-03
  - ABOUT-V2-04
  - ABOUT-V2-05
must_haves:
  truths:
    - "About section renders new title 'Quem está por trás da DMV?' with consultative intro about 3 complementary founders"
    - "Three founder cards render with photo (foto-vitor.jpg, foto-mariel.jpg, foto-daniel.jpg), founder name, and specialty line underneath"
    - "Tagline 'Driven Mind Vanguard — Vanguarda da Mente Direcionada.' displays below intro"
    - "Photos render at ~120-160px on desktop, use loading='lazy' and object-fit: cover, and remain circular/rounded across breakpoints"
    - "Toggling language swaps title, intro, tagline, and 3 specialty lines to English"
  artifacts:
    - path: "index.html"
      provides: "Restructured #about section with intro + 3 founder cards including specialty element"
      contains: "about.founder1.specialty"
    - path: "script.js"
      provides: "Updated about.* PT/EN keys + new about.intro, about.tagline, about.founderN.specialty keys"
      contains: "Estratégia & Automação"
    - path: "styles.css"
      provides: "Specialty line styling (and any adjustment to .about-inner grid to fit the new intro+bio layout)"
      contains: "founder-specialty"
  key_links:
    - from: "index.html #about section"
      to: "script.js translations[pt|en]"
      via: "data-i18n attributes for new keys (about.intro, about.tagline, about.founderN.specialty)"
      pattern: "data-i18n=\"about\\."
    - from: ".founder-card img"
      to: "foto-vitor.jpg / foto-mariel.jpg / foto-daniel.jpg (root)"
      via: "<img src> with loading=lazy"
      pattern: "foto-(vitor|mariel|daniel)\\.jpg"
---

<objective>
Restructure the Quem Somos / About section from generic mission paragraph to: new title + consultative 3-founder intro + tagline + 3 mini-bio cards (photo + name + specialty line). PT and EN both updated via i18n.

Purpose: Visitor meets the 3 founders with real photos and complementary specialty framing (strategy + tech + data) aligned with the "diagnosis born from business understanding" positioning.
Output: Updated `index.html` about section, extended `script.js` i18n, and minor `styles.css` additions for the specialty label.
</objective>

<execution_context>
@C:/Users/00amiri/Documents/DMV Ent/Projetos DMV/dmv-landing-page/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/00amiri/Documents/DMV Ent/Projetos DMV/dmv-landing-page/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/12-diferenciais-quem-somos-faq/12-CONTEXT.md

<!-- Existing #about section: index.html lines 454–487 -->
<!-- Existing PT about.* keys: script.js lines 103–107; EN: 231–235 -->
<!-- Founder cards already exist (.founder-card > .founder-photo-glow > img + .founder-name). Add .founder-specialty span underneath. -->
<!-- Root images verified present: foto-vitor.jpg, foto-mariel.jpg, foto-daniel.jpg -->
<!-- Existing about.mission-prefix / about.mission-suffix are replaced by a cleaner about.intro single key. -->
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Restructure #about HTML (intro + founder specialties)</name>
  <files>index.html</files>
  <action>
In `index.html` inside `<section id="about">` (lines 454–487):

1. Keep `<span class="section-badge" data-i18n="about.badge">` unchanged ("◆ Quem Somos" / "◆ Who We Are").

2. Keep `<h2 class="about-title" data-i18n="about.title">` text → `Quem está por trás da DMV?` (already correct on line 459 — verify).

3. REPLACE the existing `<p class="about-mission">` element (line 460, with mission-prefix/mission-suffix + bold names) with a single intro paragraph:

   ```html
   <p class="about-mission" data-i18n="about.intro">Somos três fundadores com formações complementares — estratégia, tecnologia e dados — unidos por uma convicção: IA só entrega valor quando nasce do entendimento profundo do negócio.</p>
   ```

4. REPLACE the `about-badge-wrap` block (lines 461–463) with an updated tagline using the new key `about.tagline`:

   ```html
   <div class="about-badge-wrap">
     <span class="about-seal-badge" data-i18n="about.tagline">Driven Mind Vanguard — Vanguarda da Mente Direcionada.</span>
   </div>
   ```

   (Keep the `.about-seal-badge` class; rename the i18n key from `about.seal` to `about.tagline` to match CONTEXT.md Decision D-Tagline.)

5. For each `.founder-card` (3 cards at lines 466–483), ADD a specialty `<span>` after the existing `<span class="founder-name">`:

   - Card 1 (Vitor): after `<span class="founder-name">Vitor Pellegrino</span>` add
     `<span class="founder-specialty" data-i18n="about.founder1.specialty">Estratégia &amp; Automação</span>`
   - Card 2 (Mariel): after name add
     `<span class="founder-specialty" data-i18n="about.founder2.specialty">IA &amp; Desenvolvimento</span>`
   - Card 3 (Daniel): after name add
     `<span class="founder-specialty" data-i18n="about.founder3.specialty">Análise de Dados &amp; Otimização</span>`

6. Confirm all 3 `<img>` tags already use `src="foto-{name}.jpg"`, `alt="{name}"`, and `loading="lazy"` (they do per line 468/474/480 — verify no regression).

Do NOT alter the `.about-inner` outer grid, `.about-founders-wrap`, or SVG/gradient decorations.

Implements ABOUT-V2-01 (title), ABOUT-V2-02 (intro), ABOUT-V2-03 (tagline), ABOUT-V2-04 (3 founder cards w/ photos), ABOUT-V2-05 (specialty line) per 12-CONTEXT decisions.
  </action>
  <verify>
    <automated>node -e "const h=require('fs').readFileSync('index.html','utf8'); const need=['about.intro','about.tagline','about.founder1.specialty','about.founder2.specialty','about.founder3.specialty','foto-vitor.jpg','foto-mariel.jpg','foto-daniel.jpg','forma\u00e7\u00f5es complementares','founder-specialty']; const miss=need.filter(x=>!h.includes(x)); if(miss.length){console.error('MISSING',miss);process.exit(1)}; if(h.includes('about.mission-prefix')||h.includes('about.mission-suffix')){console.error('OLD MISSION KEYS STILL PRESENT');process.exit(1)}; console.log('OK')"</automated>
  </verify>
  <done>#about section contains new intro paragraph, tagline, and 3 founder cards each with photo + name + specialty element; old mission-prefix/suffix markup removed.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update about.* i18n (PT + EN) in script.js</name>
  <files>script.js</files>
  <action>
In `script.js`:

1. In PT translations block (around lines 103–107), REMOVE `about.mission-prefix`, `about.mission-suffix`, and `about.seal` keys. ADD:

```js
'about.badge': '\u2666 Quem Somos',
'about.title': 'Quem está por trás da DMV?',
'about.intro': 'Somos três fundadores com formações complementares — estratégia, tecnologia e dados — unidos por uma convicção: IA só entrega valor quando nasce do entendimento profundo do negócio.',
'about.tagline': 'Driven Mind Vanguard — Vanguarda da Mente Direcionada.',
'about.founder1.specialty': 'Estratégia & Automação',
'about.founder2.specialty': 'IA & Desenvolvimento',
'about.founder3.specialty': 'Análise de Dados & Otimização',
```

2. In EN translations block (around lines 231–235), mirror:

```js
'about.badge': '\u2666 Who We Are',
'about.title': 'Who\'s behind DMV?',
'about.intro': 'We\'re three founders with complementary backgrounds — strategy, technology, and data — united by one conviction: AI only delivers value when it\'s born from a deep understanding of the business.',
'about.tagline': 'Driven Mind Vanguard.',
'about.founder1.specialty': 'Strategy & Automation',
'about.founder2.specialty': 'AI & Development',
'about.founder3.specialty': 'Data Analytics & Optimization',
```

Preserve existing key naming patterns. Remove any now-orphaned `about.mission-prefix/suffix/seal` lines from both PT and EN blocks.

Implements ABOUT-V2-01..05 i18n wiring per 12-CONTEXT decisions.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('script.js','utf8'); const need=['about.intro','about.tagline','about.founder1.specialty','about.founder3.specialty','forma\u00e7\u00f5es complementares','complementary backgrounds','Strategy & Automation','An\u00e1lise de Dados & Otimiza\u00e7\u00e3o']; const miss=need.filter(x=>!s.includes(x)); if(miss.length){console.error('MISSING',miss);process.exit(1)}; if(s.includes('about.mission-prefix')||s.includes('about.mission-suffix')||s.match(/'about\\.seal'/)){console.error('OLD ABOUT KEYS STILL PRESENT');process.exit(1)}; console.log('OK')"</automated>
  </verify>
  <done>PT and EN about.* keys reflect new intro/tagline/3 specialties. Old mission-prefix/mission-suffix/seal keys removed from both blocks.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Add .founder-specialty styling in styles.css</name>
  <files>styles.css</files>
  <action>
In `styles.css`, find the existing `.founder-name` rule (grep for `.founder-name` to locate its block). Immediately AFTER that rule, APPEND a new `.founder-specialty` rule that visually subordinates the specialty line under the name:

```css
.founder-specialty {
  display: block;
  margin-top: 0.25rem;
  font-family: 'JetBrains Mono', monospace;
  font-size: 0.78rem;
  letter-spacing: 0.02em;
  color: var(--color-text-muted, rgba(255, 255, 255, 0.65));
  text-align: center;
}
```

If `--color-text-muted` is not defined in :root, the fallback `rgba(255, 255, 255, 0.65)` handles it. Do NOT modify any other rule. Do NOT alter photo size / .founder-photo-glow / .founder-card layout — founder cards already render at 120–160px photos per existing design.

If the about-founders-wrap currently uses a constrained width that would crowd the new specialty line, add a subtle `min-height` or `padding-bottom` to `.founder-card` only if necessary. Default: no additional layout change needed.

Implements ABOUT-V2-05 visual styling (specialty label) per CONTEXT photo-styling guidance.
  </action>
  <verify>
    <automated>node -e "const c=require('fs').readFileSync('styles.css','utf8'); if(!c.includes('.founder-specialty')){console.error('MISSING .founder-specialty rule');process.exit(1)}; console.log('OK')"</automated>
  </verify>
  <done>.founder-specialty rule exists in styles.css and visually subordinates the specialty under the name.</done>
</task>

</tasks>

<verification>
- Load index.html in browser; scroll to #about: new intro, tagline, 3 founder cards with photo + name + specialty render.
- Toggle to EN: title, intro, tagline, 3 specialties all switch.
- Founder photos load (no 404), stay circular via existing .founder-photo-glow styling.
- Responsive check at 375px: cards stack cleanly.
</verification>

<success_criteria>
ABOUT-V2-01..05 satisfied in PT and EN. Founder photos render from root assets. Specialty line is visually present and styled.
</success_criteria>

<output>
After completion, create `.planning/phases/12-diferenciais-quem-somos-faq/12-02-SUMMARY.md`.
</output>
