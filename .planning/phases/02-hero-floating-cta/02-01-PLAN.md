---
phase: 02-hero-floating-cta
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
  - styles.css
  - script.js
autonomous: true
requirements:
  - HERO-01
  - HERO-02
  - HERO-03
  - HERO-04
  - HERO-05
  - HERO-06
  - HERO-07
  - HERO-08
  - HERO-09
  - CTA-06

must_haves:
  truths:
    - "Hero displays asymmetric layout on desktop: text 60% left, logo 40% right"
    - "Hero stacks vertically on mobile with logo centered below text"
    - "Badge with cyan mono text and glass pill background appears above the headline"
    - "Headline is the visually largest text element on the page at clamp(2.5rem, 5vw, 4.5rem)"
    - "Sub-headline paragraph appears below headline explaining the service"
    - "Primary CTA button has cyan glow and pulse animation, links to WhatsApp"
    - "Secondary CTA link scrolls to #how-it-works section"
    - "Logo on right has atmospheric radial cyan glow effect"
    - "Guarantee seal at bottom of hero shows three trust phrases with check icons"
    - "All hero CTAs resolve to https://wa.me/5522981113794"
  artifacts:
    - path: "index.html"
      provides: "Hero section HTML with badge, headline, sub-headline, CTAs, logo, seal"
      contains: "hero-content"
    - path: "styles.css"
      provides: "Hero layout CSS, typography, glow effects, responsive breakpoints"
      contains: "hero-grid"
    - path: "script.js"
      provides: "Hero i18n translation keys for all text elements"
      contains: "hero.badge"
  key_links:
    - from: "index.html"
      to: "styles.css"
      via: "hero CSS classes"
      pattern: "hero-grid|hero-badge|hero-headline|hero-cta"
    - from: "index.html"
      to: "script.js"
      via: "data-i18n attributes"
      pattern: "data-i18n=\"hero\\."
    - from: "index.html (hero CTA)"
      to: "https://wa.me/5522981113794"
      via: "href attribute"
      pattern: "wa\\.me/5522981113794"
---

<objective>
Build the complete hero section with asymmetric layout, all text elements (badge, headline, sub-headline), primary and secondary CTAs, logo with atmospheric glow, guarantee seal, and full responsive behavior. Add all hero i18n keys to the translations object.

Purpose: The hero is the most critical conversion section — 70% of conversions start here. It must communicate the value proposition instantly and make the WhatsApp CTA impossible to miss.
Output: Fully styled hero section visible at all breakpoints, all text bilingual, primary CTA linking to WhatsApp.
</objective>

<execution_context>
@C:/Users/00amiri/Documents/DMV Ent/Projetos DMV/dmv-landing-page/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/00amiri/Documents/DMV Ent/Projetos DMV/dmv-landing-page/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-02-SUMMARY.md

<interfaces>
<!-- Existing CSS design tokens from styles.css :root -->
--color-primary: #040810;
--color-cyan: #00c8ff;
--color-blue: #1a4fd4;
--color-purple: #6b3fa0;
--color-text: #e8ecf4;
--color-text-secondary: #8a95a8;
--color-text-muted: #5a6578;
--color-border: #1a2440;
--glass-bg: rgba(255, 255, 255, 0.04);
--glass-border: rgba(255, 255, 255, 0.08);
--glow-cyan: 0 0 40px rgba(0, 200, 255, 0.25), 0 0 80px rgba(0, 200, 255, 0.1);
--font-headline: 'Sora', sans-serif;
--font-body: 'DM Sans', sans-serif;
--font-mono: 'JetBrains Mono', monospace;
--section-padding: clamp(60px, 10vw, 140px);
--container-max: 1200px;
--container-padding: clamp(16px, 4vw, 40px);
--navbar-height: 72px;
--transition-fast: 0.2s ease;
--transition-default: 0.3s ease;

<!-- Existing hero HTML stub in index.html (lines 57-61) -->
<section id="hero" class="section section-hero">
  <div class="container">
    <h2 data-i18n="hero.title">Seu negocio no piloto automatico com Inteligencia Artificial</h2>
  </div>
</section>

<!-- Existing hero CSS in styles.css (lines 525-529) -->
.section-hero {
  min-height: 100vh;
  display: flex;
  align-items: center;
}

<!-- Existing i18n keys in script.js translations object -->
'hero.title' already exists in both PT and EN
setLanguage() function already works with data-i18n attributes
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace hero HTML stub with full hero section content</name>
  <files>index.html</files>
  <read_first>index.html</read_first>
  <action>
Replace the existing hero section (lines 57-61 in index.html) with the full hero content. The structure must be:

```html
<section id="hero" class="section section-hero">
  <div class="container hero-grid">
    <!-- Left column: text content (60% desktop) -->
    <div class="hero-content">
      <span class="hero-badge" data-i18n="hero.badge">&#10022; Agencia de Automacao com IA</span>
      <h1 class="hero-headline" data-i18n="hero.headline">Seu negocio no piloto automatico com Inteligencia Artificial</h1>
      <p class="hero-subheadline" data-i18n="hero.subheadline">Criamos assistentes virtuais e automacoes sob medida que atendem seus clientes 24h, organizam seus processos e liberam seu tempo para o que realmente importa.</p>
      <div class="hero-ctas">
        <a href="https://wa.me/5522981113794" class="btn-cta-hero" data-i18n="hero.cta-primary" target="_blank" rel="noopener">Agendar Demonstracao Gratuita</a>
        <a href="#how-it-works" class="hero-cta-secondary" data-i18n="hero.cta-secondary">Ver como funciona &#8595;</a>
      </div>
      <div class="hero-seal">
        <span class="hero-seal-item"><svg width="16" height="16" viewBox="0 0 16 16" fill="none" aria-hidden="true"><path d="M13.3 4.3L6 11.6 2.7 8.3" stroke="#00c8ff" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/></svg> <span data-i18n="hero.seal-1">Demonstracao 100% gratuita</span></span>
        <span class="hero-seal-item"><svg width="16" height="16" viewBox="0 0 16 16" fill="none" aria-hidden="true"><path d="M13.3 4.3L6 11.6 2.7 8.3" stroke="#00c8ff" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/></svg> <span data-i18n="hero.seal-2">Sem compromisso</span></span>
        <span class="hero-seal-item"><svg width="16" height="16" viewBox="0 0 16 16" fill="none" aria-hidden="true"><path d="M13.3 4.3L6 11.6 2.7 8.3" stroke="#00c8ff" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/></svg> <span data-i18n="hero.seal-3">Resposta imediata</span></span>
      </div>
    </div>
    <!-- Right column: logo with glow (40% desktop) -->
    <div class="hero-visual">
      <div class="hero-logo-glow">
        <img src="Logo_dmv.jpg" alt="DMV Enterprise - Driven Mind Vanguard" class="hero-logo-img" width="320" height="320">
      </div>
    </div>
  </div>
</section>
```

IMPORTANT NOTES:
- Change the old `h2` data-i18n key from "hero.title" to match the new key "hero.headline" (per HERO-03).
- The badge uses `&#10022;` for the star character (Unicode ✦).
- The secondary CTA uses `&#8595;` for the down arrow (Unicode ↓).
- The checkmark SVGs are inline (16x16) with stroke color #00c8ff (var(--color-cyan)).
- Primary CTA href MUST be `https://wa.me/5522981113794` per CTA-06.
- Secondary CTA href is `#how-it-works` for smooth scroll per HERO-06.
- Every text element has a data-i18n attribute.
- The logo img uses the existing Logo_dmv.jpg file with width and height attributes for layout stability.
  </action>
  <verify>
    <automated>grep -c "hero-grid" index.html | grep -q "1" && grep -c "hero-badge" index.html | grep -q "1" && grep -c "hero-headline" index.html | grep -q "1" && grep -c "hero-subheadline" index.html | grep -q "1" && grep -c "btn-cta-hero" index.html | grep -q "1" && grep -c "hero-seal" index.html | grep -q "1" && grep -c "hero-logo-glow" index.html | grep -q "1" && grep -c "wa.me/5522981113794" index.html | grep -qE "[2-9]" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - index.html contains `hero-grid` class (1 occurrence)
    - index.html contains `hero-badge` class (1 occurrence)
    - index.html contains `hero-headline` tag with data-i18n="hero.headline"
    - index.html contains `hero-subheadline` with data-i18n
    - index.html contains `btn-cta-hero` linking to wa.me/5522981113794
    - index.html contains `hero-seal` with 3 seal items
    - index.html contains `hero-logo-glow` wrapper
    - index.html has at least 2 instances of wa.me/5522981113794 (nav CTA + hero CTA)
  </acceptance_criteria>
  <done>Hero section in index.html contains badge, h1 headline, sub-headline paragraph, primary CTA to WhatsApp, secondary CTA to #how-it-works, logo with glow wrapper, and guarantee seal with 3 check items. All text elements have data-i18n attributes.</done>
</task>

<task type="auto">
  <name>Task 2: Add hero CSS styles with layout, typography, glow effects, and responsive breakpoints</name>
  <files>styles.css</files>
  <read_first>styles.css</read_first>
  <action>
Append the following CSS to styles.css, AFTER the existing `.section-hero` rules (after line 529). Replace the existing `.section-hero` block with the updated version and add all new hero component styles.

First, REPLACE the existing `.section-hero` block (lines 525-529) with:
```css
.section-hero {
  min-height: 100vh;
  display: flex;
  align-items: center;
  padding-top: calc(var(--navbar-height) + 40px);
  padding-bottom: 60px;
}
```

Then APPEND these new rules after it:

```css
/* ============================================
   HERO SECTION
   ============================================ */

/* --- Hero Grid: 60/40 asymmetric layout (HERO-01) --- */

.hero-grid {
  display: grid;
  grid-template-columns: 1fr 0.65fr;
  gap: 48px;
  align-items: center;
}

/* --- Hero Content (left column) --- */

.hero-content {
  display: flex;
  flex-direction: column;
  gap: 24px;
}

/* --- Badge (HERO-02) --- */

.hero-badge {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  font-family: var(--font-mono);
  font-size: 0.85rem;
  font-weight: 700;
  color: var(--color-cyan);
  background: rgba(0, 200, 255, 0.08);
  border: 1px solid rgba(0, 200, 255, 0.15);
  padding: 8px 16px;
  border-radius: 100px;
  width: fit-content;
  letter-spacing: 0.02em;
}

/* --- Headline (HERO-03) --- */

.hero-headline {
  font-family: var(--font-headline);
  font-size: clamp(2.5rem, 5vw, 4.5rem);
  font-weight: 800;
  line-height: 1.1;
  letter-spacing: -0.02em;
  color: var(--color-text);
  margin: 0;
}

/* --- Sub-headline (HERO-04) --- */

.hero-subheadline {
  font-family: var(--font-body);
  font-size: clamp(1rem, 1.5vw, 1.2rem);
  line-height: 1.7;
  color: var(--color-text-secondary);
  max-width: 540px;
  margin: 0;
}

/* --- CTAs container (HERO-05, HERO-06) --- */

.hero-ctas {
  display: flex;
  align-items: center;
  gap: 24px;
  flex-wrap: wrap;
}

/* --- Primary CTA button with glow (HERO-05) --- */

.btn-cta-hero {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  padding: 16px 32px;
  background: var(--color-cyan);
  color: var(--color-primary);
  font-family: var(--font-headline);
  font-weight: 700;
  font-size: 1rem;
  border-radius: 12px;
  border: none;
  cursor: pointer;
  text-decoration: none;
  white-space: nowrap;
  box-shadow: 0 0 30px rgba(0, 200, 255, 0.3), 0 0 60px rgba(0, 200, 255, 0.1);
  transition: box-shadow var(--transition-default), transform var(--transition-fast);
  animation: cta-pulse 2.5s ease-in-out infinite;
}

.btn-cta-hero:hover {
  color: var(--color-primary);
  box-shadow: 0 0 50px rgba(0, 200, 255, 0.5), 0 0 100px rgba(0, 200, 255, 0.2);
  transform: translateY(-2px);
}

@keyframes cta-pulse {
  0%, 100% { transform: scale(1); }
  50% { transform: scale(1.03); }
}

/* --- Secondary CTA link (HERO-06) --- */

.hero-cta-secondary {
  color: var(--color-text-secondary);
  font-size: 0.95rem;
  font-weight: 500;
  text-decoration: none;
  transition: color var(--transition-fast);
  display: inline-flex;
  align-items: center;
  gap: 4px;
}

.hero-cta-secondary:hover {
  color: var(--color-cyan);
}

/* --- Logo visual with atmospheric glow (HERO-07) --- */

.hero-visual {
  display: flex;
  align-items: center;
  justify-content: center;
}

.hero-logo-glow {
  position: relative;
  display: flex;
  align-items: center;
  justify-content: center;
}

.hero-logo-glow::before {
  content: '';
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  width: 110%;
  height: 110%;
  background: radial-gradient(circle, rgba(0, 200, 255, 0.2) 0%, rgba(0, 200, 255, 0.05) 40%, transparent 70%);
  border-radius: 50%;
  filter: blur(30px);
  z-index: 0;
  pointer-events: none;
}

.hero-logo-img {
  position: relative;
  z-index: 1;
  max-width: 320px;
  width: 100%;
  height: auto;
  border-radius: 24px;
  box-shadow: 0 0 60px rgba(0, 200, 255, 0.15), 0 0 120px rgba(0, 200, 255, 0.05);
}

/* --- Guarantee seal (HERO-08) --- */

.hero-seal {
  display: flex;
  flex-wrap: wrap;
  gap: 16px 24px;
  padding-top: 8px;
}

.hero-seal-item {
  display: inline-flex;
  align-items: center;
  gap: 6px;
  font-size: 0.8rem;
  color: var(--color-text-muted);
}

.hero-seal-item svg {
  flex-shrink: 0;
}

/* --- Hero responsive (HERO-09) --- */

@media (max-width: 1024px) {
  .hero-grid {
    grid-template-columns: 1fr 0.5fr;
    gap: 32px;
  }

  .hero-logo-img {
    max-width: 260px;
  }
}

@media (max-width: 768px) {
  .hero-grid {
    grid-template-columns: 1fr;
    gap: 40px;
    text-align: center;
  }

  .hero-content {
    align-items: center;
  }

  .hero-subheadline {
    max-width: 100%;
  }

  .hero-ctas {
    justify-content: center;
  }

  .hero-visual {
    order: 1;
  }

  .hero-content {
    order: 0;
  }

  .hero-logo-img {
    max-width: 220px;
  }

  .hero-seal {
    justify-content: center;
  }

  .btn-cta-hero {
    width: 100%;
    max-width: 320px;
    padding: 18px 32px;
  }
}

@media (max-width: 375px) {
  .hero-headline {
    font-size: 2.2rem;
  }

  .hero-seal {
    flex-direction: column;
    align-items: center;
    gap: 8px;
  }
}
```

IMPORTANT DETAILS:
- The grid uses `1fr 0.65fr` to achieve the ~60/40 split per HERO-01.
- The headline font-size uses `clamp(2.5rem, 5vw, 4.5rem)` per the design spec (HERO-03).
- The CTA pulse animation uses `scale(1) -> scale(1.03) -> scale(1)` at 2.5s interval per HERO-05.
- The logo glow uses a `::before` pseudo-element with radial-gradient + blur(30px) per HERO-07.
- The logo image also gets box-shadow glow for the "floating in space" effect.
- At 768px, the grid collapses to single column with text centered and logo below (HERO-09).
- At 375px, headline shrinks further and seal items stack vertically.
  </action>
  <verify>
    <automated>grep -c "hero-grid" styles.css | grep -qE "[1-9]" && grep -c "hero-badge" styles.css | grep -qE "[1-9]" && grep -c "hero-headline" styles.css | grep -qE "[1-9]" && grep -c "btn-cta-hero" styles.css | grep -qE "[1-9]" && grep -c "cta-pulse" styles.css | grep -qE "[1-9]" && grep -c "hero-logo-glow" styles.css | grep -qE "[1-9]" && grep -c "hero-seal" styles.css | grep -qE "[1-9]" && grep "max-width: 768px" styles.css | grep -q "hero-grid" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - styles.css contains `.hero-grid` with grid-template-columns
    - styles.css contains `.hero-badge` with font-mono and cyan color
    - styles.css contains `.hero-headline` with clamp(2.5rem, 5vw, 4.5rem)
    - styles.css contains `.btn-cta-hero` with box-shadow glow
    - styles.css contains `@keyframes cta-pulse` animation
    - styles.css contains `.hero-logo-glow::before` with radial-gradient
    - styles.css contains `.hero-seal` flex layout
    - styles.css contains 768px media query that sets `.hero-grid` to single column
  </acceptance_criteria>
  <done>Hero CSS is complete with asymmetric grid layout, badge glass pill styling, headline at clamp(2.5rem, 5vw, 4.5rem) weight 800, primary CTA with cyan glow and pulse animation, secondary CTA link style, logo atmospheric glow via pseudo-element, guarantee seal layout, and responsive breakpoints at 1024px, 768px, and 375px.</done>
</task>

<task type="auto">
  <name>Task 3: Add hero i18n translation keys to script.js</name>
  <files>script.js</files>
  <read_first>script.js</read_first>
  <action>
Update the translations object in script.js. The existing `'hero.title'` key must be REPLACED with the new hero keys. In BOTH the `pt` and `en` objects inside `translations`:

1. REMOVE the old key `'hero.title'` from both pt and en objects.

2. ADD these keys to the `pt` object (after the `'nav.cta'` line, before the section titles block):

```javascript
    // Hero section
    'hero.badge': '\u2726 Agencia de Automacao com IA',
    'hero.headline': 'Seu negocio no piloto automatico com Inteligencia Artificial',
    'hero.subheadline': 'Criamos assistentes virtuais e automacoes sob medida que atendem seus clientes 24h, organizam seus processos e liberam seu tempo para o que realmente importa.',
    'hero.cta-primary': 'Agendar Demonstracao Gratuita',
    'hero.cta-secondary': 'Ver como funciona \u2193',
    'hero.seal-1': 'Demonstracao 100% gratuita',
    'hero.seal-2': 'Sem compromisso',
    'hero.seal-3': 'Resposta imediata',
```

3. ADD these keys to the `en` object (same position):

```javascript
    // Hero section
    'hero.badge': '\u2726 AI Automation Agency',
    'hero.headline': 'Your business on autopilot with Artificial Intelligence',
    'hero.subheadline': 'We build custom virtual assistants and automations that serve your customers 24/7, streamline your operations, and free your time for what truly matters.',
    'hero.cta-primary': 'Schedule Free Demo',
    'hero.cta-secondary': 'See how it works \u2193',
    'hero.seal-1': 'Free demo, no strings attached',
    'hero.seal-2': 'No commitment required',
    'hero.seal-3': 'Immediate response',
```

IMPORTANT:
- Use unicode escapes `\u2726` for the star (✦) and `\u2193` for the arrow (↓) to avoid encoding issues in JS.
- The EN translations must sound natural, not literal (per I18N-04). "Free demo, no strings attached" is more natural than "100% free demonstration".
- Keep all existing keys intact (nav, section titles, footer).
- The old `'hero.title'` key is removed because the HTML now uses `hero.headline` instead.
  </action>
  <verify>
    <automated>grep -c "hero.badge" script.js | grep -qE "[2-9]" && grep -c "hero.headline" script.js | grep -qE "[2-9]" && grep -c "hero.subheadline" script.js | grep -qE "[2-9]" && grep -c "hero.cta-primary" script.js | grep -qE "[2-9]" && grep -c "hero.cta-secondary" script.js | grep -qE "[2-9]" && grep -c "hero.seal-1" script.js | grep -qE "[2-9]" && ! grep -q "hero.title" script.js && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - script.js contains `hero.badge` in both PT and EN (2+ occurrences)
    - script.js contains `hero.headline` in both PT and EN (2+ occurrences)
    - script.js contains `hero.subheadline` in both PT and EN (2+ occurrences)
    - script.js contains `hero.cta-primary` in both PT and EN (2+ occurrences)
    - script.js contains `hero.cta-secondary` in both PT and EN (2+ occurrences)
    - script.js contains `hero.seal-1`, `hero.seal-2`, `hero.seal-3` in both PT and EN
    - script.js does NOT contain the old `hero.title` key (replaced by `hero.headline`)
  </acceptance_criteria>
  <done>Translations object has 8 new hero keys (badge, headline, subheadline, cta-primary, cta-secondary, seal-1, seal-2, seal-3) in both PT and EN. Old hero.title key is removed. EN translations use natural phrasing.</done>
</task>

</tasks>

<verification>
After all 3 tasks complete, verify:

1. Open index.html in a browser at 1440px width:
   - Hero shows 60/40 grid: text left, logo with glow right
   - Badge pill appears above headline in cyan mono font
   - Headline is the largest text on the page
   - Sub-headline text in muted color below headline
   - Primary CTA button has cyan glow and subtle pulse animation
   - "Ver como funciona" link appears next to CTA
   - Logo has atmospheric radial glow effect
   - Three seal items with check icons at bottom

2. Resize to 375px:
   - Content stacks vertically, text centered
   - Logo appears below text content
   - CTA button is full-width
   - Seal items stack vertically

3. Click the primary CTA — must open https://wa.me/5522981113794
4. Click "Ver como funciona" — must smooth scroll to #how-it-works section
5. Toggle language to EN — all hero text switches to natural English
6. Toggle back to PT — all hero text returns to Portuguese
</verification>

<success_criteria>
- Hero section renders correctly at 1440px, 1024px, 768px, and 375px
- Asymmetric 60/40 layout on desktop, stacked on mobile
- All 8 i18n keys work in both PT and EN
- Primary CTA links to WhatsApp URL
- Secondary CTA scrolls to #how-it-works
- Logo glow effect is visible
- CTA has pulse animation
- Headline is visually the largest element on page
</success_criteria>

<output>
After completion, create `.planning/phases/02-hero-floating-cta/02-01-SUMMARY.md`
</output>
